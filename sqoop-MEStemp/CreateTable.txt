
CREATE DATABASE IF NOT EXISTS MEStemp;

DROP TABLE IF EXISTS MEStemp.Underscore_SP_Crate;

Create External table IF NOT EXISTS MEStemp.Underscore_SP_Crate (CrateID bigint NOT NULL, CrateNo nvarchar(508) NOT NULL, PartID int NOT NULL, Quantity float NOT NULL, CurrentQuantity float NOT NULL, Locked tinyint NOT NULL, Complete tinyint NOT NULL, CreatedDT datetime NOT NULL, CompletedDT datetime NOT NULL, MachineStageID smallint NOT NULL, CrateIndex bigint NOT NULL, MirrorPartID int NOT NULL, PadLock uniqueidentifier,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Underscore_SP_Crate/';

DROP TABLE IF EXISTS MEStemp.Underscore_SP_CrateLabel;

Create External table IF NOT EXISTS MEStemp.Underscore_SP_CrateLabel (_SP_CrateLabelID bigint NOT NULL, CrateID bigint NOT NULL, MachineStage nvarchar(508), PartNumber nvarchar(508), InvState nvarchar(508), CrateNo nvarchar(508), CreatedDT datetime, CompleteDT datetime, Quantity float, LabelTemplate nvarchar(508), PrinterURL nvarchar(2048) NOT NULL, PrintedDT datetime NOT NULL, ScannedDT datetime NOT NULL, IsCancelled tinyint NOT NULL, IsReprint tinyint NOT NULL, PartDescription nvarchar(508), LabelSaveDirectory nvarchar(508), IsPrintLandscape tinyint NOT NULL, NoCopies smallint NOT NULL, LabelsPerInventoryPack smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Underscore_SP_CrateLabel/';

DROP TABLE IF EXISTS MEStemp.Underscore_SP_LkInvIsolatedReasonMST;

Create External table IF NOT EXISTS MEStemp.Underscore_SP_LkInvIsolatedReasonMST (LkInvIsolatedReasonMSTID int NOT NULL, InvIsolatedReasonID smallint NOT NULL, MachineStageTypeID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Underscore_SP_LkInvIsolatedReasonMST/';

DROP TABLE IF EXISTS MEStemp.Underscore_SP_Pallet;

Create External table IF NOT EXISTS MEStemp.Underscore_SP_Pallet (PalletID bigint NOT NULL, PalletNo nvarchar(508) NOT NULL, PartID int NOT NULL, InvStateID smallint NOT NULL, Quantity float NOT NULL, CurrentQuantity float NOT NULL, Locked tinyint NOT NULL, Complete tinyint NOT NULL, CreatedDT datetime NOT NULL, CompletedDT datetime NOT NULL, MachineStageID smallint NOT NULL, PrincipleWOID int, PalletIndex bigint NOT NULL, ModulePartID int NOT NULL, Merged tinyint NOT NULL, PadLock uniqueidentifier, IsBtoCto tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Underscore_SP_Pallet/';

DROP TABLE IF EXISTS MEStemp.Underscore_SP_PalletLabel;

Create External table IF NOT EXISTS MEStemp.Underscore_SP_PalletLabel (_SP_PalletLabelID bigint NOT NULL, PalletID bigint NOT NULL, MachineStage nvarchar(508), PartNumber nvarchar(508), InvState nvarchar(508), PalletNo nvarchar(508), CreatedDT datetime, CompleteDT datetime, Quantity float, LabelTemplate nvarchar(508), PrinterURL nvarchar(2048) NOT NULL, PrintedDT datetime NOT NULL, ScannedDT datetime NOT NULL, IsCancelled tinyint NOT NULL, IsReprint tinyint NOT NULL, PartDescription nvarchar(508), LabelSaveDirectory nvarchar(508), IsPrintLandscape tinyint NOT NULL, NoCopies smallint NOT NULL, LabelsPerInventoryPack smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Underscore_SP_PalletLabel/';

DROP TABLE IF EXISTS MEStemp.Underscore_SP_SEQ_LPN;

Create External table IF NOT EXISTS MEStemp.Underscore_SP_SEQ_LPN (SeqID int NOT NULL, SeqVal varchar(1) NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Underscore_SP_SEQ_LPN/';

DROP TABLE IF EXISTS MEStemp.Underscore_SPML_ModuleInfo;

Create External table IF NOT EXISTS MEStemp.Underscore_SPML_ModuleInfo (ModuleInfoID int NOT NULL, SerialNo nvarchar(508) NOT NULL, LaminateID int NOT NULL, LaminateCreatedDT datetime NOT NULL, LaminateOrderNo nvarchar(508) NOT NULL, LaminatePartNo nvarchar(508) NOT NULL, BINNo nvarchar(508) NOT NULL, CktFormBuildPart nvarchar(508), CktFormBuildMachine nvarchar(508), CktFormBuildDT datetime, GlassBuildPart nvarchar(508), GlassBuildDate datetime, LaminationBuildMachine nvarchar(508), LaminationBuildDT datetime, ModuleID int, ModuleCreatedDT datetime, ModuleOrderNo nvarchar(508), IntendedModulePartNo nvarchar(508), IntendedModulePartDesc nvarchar(508), FramingBuildPart nvarchar(508), FramingBuildMachine nvarchar(508), FramingBuildDT datetime, JboxBuildPart nvarchar(508), JboxBuildDT datetime, FlashtestClassifiedDT datetime, FinalModulePartNo nvarchar(508), FinalModulePartDesc nvarchar(508), ActualPower float, ActualVocMod float, ActualFF float, PackedDT datetime, InventoryState nvarchar(508), JboxBuildMachine nvarchar(508), ActualIsc float, FlashtestBuildMachine nvarchar(508), Komax1 nvarchar(508), Komax2 nvarchar(508), Komax3 nvarchar(508), GlassID int, CellType nvarchar(100), StringerOperator nvarchar(100), CircuitOperator1 nvarchar(100), CircuitOperator2 nvarchar(100), TrimChangeReason nvarchar(4000), PowerChartGenerated tinyint NOT NULL, LaminateWoId int, ModuleWoId int, VisualStateId smallint, RfInvStateId smallint, ElTestDate datetime, ElBuildMachine nvarchar(508), ActualVmpMod float, ActualImpMod float, TrimBuildMachine nvarchar(508), TrimBuildDT datetime, CellThickness nvarchar(508), PalletID bigint, FlashTestID int, PadLock uniqueidentifier, BtoCtoSerialNo nvarchar(508), EvaUseByDT datetime, IsBtoCto tinyint, IntendedModuleWOID int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Underscore_SPML_ModuleInfo/';

DROP TABLE IF EXISTS MEStemp.AcqCtxVarClass;

Create External table IF NOT EXISTS MEStemp.AcqCtxVarClass (AcqCtxVarClassID int NOT NULL, AcquisitionContextID int NOT NULL, VariableTypeClassID tinyint NOT NULL, IsOS tinyint NOT NULL, IsOC tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/AcqCtxVarClass/';

DROP TABLE IF EXISTS MEStemp.Acquisition;

Create External table IF NOT EXISTS MEStemp.Acquisition (AcquisitionID smallint NOT NULL, Acquisition nvarchar(508) NOT NULL, AcquisitionCode nvarchar(100), Description nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Acquisition/';

DROP TABLE IF EXISTS MEStemp.AcquisitionAttributeType;

Create External table IF NOT EXISTS MEStemp.AcquisitionAttributeType (AcquisitionAttributeTypeID int NOT NULL, AcquisitionVariantID smallint NOT NULL, AttributeTypeID smallint NOT NULL, EntryOrder smallint NOT NULL, Formula nvarchar(2000), IsExplicitStep tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/AcquisitionAttributeType/';

DROP TABLE IF EXISTS MEStemp.AcquisitionChain;

Create External table IF NOT EXISTS MEStemp.AcquisitionChain (AcquisitionChainID smallint NOT NULL, AcquisitionChain nvarchar(508) NOT NULL, Description nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/AcquisitionChain/';

DROP TABLE IF EXISTS MEStemp.AcquisitionContext;

Create External table IF NOT EXISTS MEStemp.AcquisitionContext (MachineStageID smallint NOT NULL, AcquisitionVariantID smallint NOT NULL, LastMeasurementSetID int, AcquisitionContextID int NOT NULL, LastDoneDT datetime, IsAttrOC tinyint NOT NULL, IsCriticalDefect tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/AcquisitionContext/';

DROP TABLE IF EXISTS MEStemp.AcquisitionDevice;

Create External table IF NOT EXISTS MEStemp.AcquisitionDevice (AcquisitionDeviceID int NOT NULL, AcquisitionVariableTypeID int NOT NULL, WorkstationConfigID smallint NOT NULL, Instructions nvarchar(-1), ImageURI nvarchar(-1), RfSourceID tinyint NOT NULL, DataDeviceID int NOT NULL, DeviceArgs nvarchar(508), DeviceOutput nvarchar(100), FillMode char(3) NOT NULL, Preset nvarchar(100) NOT NULL, Multiplier nvarchar(100) NOT NULL, PreprocessingFunction nvarchar(508), IsManualTrigger tinyint NOT NULL, IsManualConfirm tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/AcquisitionDevice/';

DROP TABLE IF EXISTS MEStemp.AcquisitionGroup;

Create External table IF NOT EXISTS MEStemp.AcquisitionGroup (AcquisitionGroupID smallint NOT NULL, AcquisitionGroup nvarchar(508) NOT NULL, Description nvarchar(508), AcquisitionGroupCode nvarchar(100), LoginID smallint NOT NULL, ModifiedDT datetime NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/AcquisitionGroup/';

DROP TABLE IF EXISTS MEStemp.AcquisitionVariableType;

Create External table IF NOT EXISTS MEStemp.AcquisitionVariableType (AcquisitionVariableTypeID int NOT NULL, AcquisitionVariantID smallint NOT NULL, VariableTypeID int NOT NULL, NoHeads smallint NOT NULL, NoSamples smallint NOT NULL, NoPositions smallint NOT NULL, EntryOrder smallint NOT NULL, Formula nvarchar(2000),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/AcquisitionVariableType/';

DROP TABLE IF EXISTS MEStemp.AcquisitionVariant;

Create External table IF NOT EXISTS MEStemp.AcquisitionVariant (AcquisitionVariantID smallint NOT NULL, AcquisitionID smallint NOT NULL, MeasurementSetTagProfileID smallint NOT NULL, PartInstanceTagProfileID smallint NOT NULL, DefectTagProfileID smallint NOT NULL, RfAcquisitionClassID tinyint NOT NULL, RfDataCollectionFreqID tinyint NOT NULL, DataCollectionPeriod int NOT NULL, IsRetired tinyint NOT NULL, AcqPlan varchar(4000) NOT NULL, VMTPID smallint NOT NULL, CauseActionTPID smallint NOT NULL, Variant nvarchar(508) NOT NULL, PartTypeID smallint NOT NULL, RevisionNo smallint NOT NULL, IsEdited tinyint NOT NULL, HeadAlias nvarchar(40) NOT NULL, CauseTagListID smallint, ActionTagListID smallint, IsMeasuredWhenProduced tinyint NOT NULL, IsInvPackCheck tinyint NOT NULL, IsEBATrigger tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/AcquisitionVariant/';

DROP TABLE IF EXISTS MEStemp.AcquisitionVariant_;

Create External table IF NOT EXISTS MEStemp.AcquisitionVariant_ (AcquisitionVariantID smallint NOT NULL, FromDT datetime NOT NULL, ToDT datetime NOT NULL, DataEntryURI nvarchar(508), SPCURI nvarchar(508), AcquisitionImageURI nvarchar(508), LoginID smallint NOT NULL, ModifiedDT datetime NOT NULL, Comments nvarchar(508), Instructions nvarchar(-1), PartID int, Variant1 nvarchar(200), Variant2 nvarchar(200), Variant3 nvarchar(200), Variant4 nvarchar(200), Variant5 nvarchar(200), Variant6 nvarchar(200), Variant7 nvarchar(200), Variant8 nvarchar(200), Variant9 nvarchar(200), Variant10 nvarchar(200), Variant11 nvarchar(200), Variant12 nvarchar(200), Variant13 nvarchar(200), Variant14 nvarchar(200), Variant15 nvarchar(200), ToolTypeID smallint, NoCavities tinyint, Documents nvarchar(-1), IsSuspendable tinyint NOT NULL, IsRotatableStepPlan tinyint NOT NULL, EBAReference nvarchar(100), Variant16 nvarchar(200), Variant17 nvarchar(200), Variant18 nvarchar(200), Variant19 nvarchar(200), Variant20 nvarchar(200),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/AcquisitionVariant_/';

DROP TABLE IF EXISTS MEStemp.Action;

Create External table IF NOT EXISTS MEStemp.Action (ActionID int NOT NULL, RfSpecialActionTypeID int NOT NULL, ActionTypeID smallint NOT NULL, ActionSubTypeID smallint NOT NULL, ActionPlanID smallint, ActionPlanCode nvarchar(508), ActionName nvarchar(508) NOT NULL, Description nvarchar(2000), ActionOrder smallint, RfPriorityID tinyint, SubmitterID smallint NOT NULL, SubmittedDT datetime NOT NULL, OwnerID smallint NOT NULL, ActionStatusID tinyint NOT NULL, ClosedDT datetime NOT NULL, ModifiedDT datetime NOT NULL, LoginID smallint NOT NULL, IsEdited tinyint NOT NULL, RevisionNo tinyint NOT NULL, DueDT datetime NOT NULL, DeadlineDT datetime NOT NULL, ExpiryDT datetime NOT NULL, SortOrder float NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Action/';

DROP TABLE IF EXISTS MEStemp.ActionNote;

Create External table IF NOT EXISTS MEStemp.ActionNote (ActionNoteID int NOT NULL, ActionID int NOT NULL, Note nvarchar(508) NOT NULL, Comments nvarchar(508), NoteDT datetime NOT NULL, LoginID smallint NOT NULL, Reference1 nvarchar(508), Reference2 nvarchar(508), Reference3 nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ActionNote/';

DROP TABLE IF EXISTS MEStemp.ActionPlan;

Create External table IF NOT EXISTS MEStemp.ActionPlan (ActionPlanID smallint NOT NULL, RfSpecialActionTypeID int NOT NULL, ActionTypeID smallint NOT NULL, ActionSubTypeID smallint NOT NULL, ActionPlanCode nvarchar(508), ActionName nvarchar(508) NOT NULL, Description nvarchar(2000), ActionPlanOrder smallint NOT NULL, RfPriorityID tinyint, IsRetired tinyint NOT NULL, VariableTypeID int, AttributeTypeID int, IsCompulsory tinyint NOT NULL, SortOrder float NOT NULL, IsOCApplicable tinyint NOT NULL, IsOSApplicable tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ActionPlan/';

DROP TABLE IF EXISTS MEStemp.ActionStatus;

Create External table IF NOT EXISTS MEStemp.ActionStatus (ActionStatusID tinyint NOT NULL, ActionStatus nvarchar(508) NOT NULL, ConditionalFormat nvarchar(508), IsClosedState tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ActionStatus/';

DROP TABLE IF EXISTS MEStemp.ActionSubType;

Create External table IF NOT EXISTS MEStemp.ActionSubType (ActionSubTypeID smallint NOT NULL, ActionSubType nvarchar(508) NOT NULL, ActionTypeID smallint NOT NULL, Description nvarchar(508), SubActionTypeCode nvarchar(508), ReportOrder smallint NOT NULL, Format nvarchar(508), IsRetired tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ActionSubType/';

DROP TABLE IF EXISTS MEStemp.ActionType;

Create External table IF NOT EXISTS MEStemp.ActionType (ActionTypeID smallint NOT NULL, ActionType nvarchar(508) NOT NULL, Description nvarchar(508), ActionTypeCode nvarchar(508), ReportOrder smallint NOT NULL, Format nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ActionType/';

DROP TABLE IF EXISTS MEStemp.Address;

Create External table IF NOT EXISTS MEStemp.Address (AddressID smallint NOT NULL, Address1 nvarchar(508), Address2 nvarchar(508), City nvarchar(100), County nvarchar(100), PostCode nvarchar(100), Country nvarchar(100),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Address/';

DROP TABLE IF EXISTS MEStemp.Asset;

Create External table IF NOT EXISTS MEStemp.Asset (AssetID int NOT NULL, AssetName nvarchar(508) NOT NULL, RfAssetTypeID smallint, LineID smallint, MachineStageID smallint, ToolID int, GaugeID smallint, PartID int, InventoryPackID int, LocationID int, IsRetired tinyint NOT NULL, CostCentreID smallint NOT NULL, ManufacturerID smallint NOT NULL, SupplierID smallint NOT NULL, AcquiredDT datetime, PONumber nvarchar(100), OrderedDT datetime, ExpectedDeliveryDT datetime, ExpectedFirstUseDT datetime, EndUseDT datetime, EndUseReason nvarchar(100), PurchaseCost float NOT NULL, DefaultLocationID int NOT NULL, Comments nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Asset/';

DROP TABLE IF EXISTS MEStemp.AssetCountType;

Create External table IF NOT EXISTS MEStemp.AssetCountType (AssetCountTypeID smallint NOT NULL, AssetCountType nvarchar(100) NOT NULL, Description nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/AssetCountType/';

DROP TABLE IF EXISTS MEStemp.AssetCycleCount;

Create External table IF NOT EXISTS MEStemp.AssetCycleCount (AssetCycleCountID int NOT NULL, AssetID int NOT NULL, RfDayPeriodID int NOT NULL, Count int NOT NULL, AssetCountTypeID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/AssetCycleCount/';

DROP TABLE IF EXISTS MEStemp.AssetExplosion;

Create External table IF NOT EXISTS MEStemp.AssetExplosion (AssetID int, ParentAssetID int, SubAssetID int, Lvl int, SortOrder varchar(254), Comments nvarchar(508), AssetLevelID int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/AssetExplosion/';

DROP TABLE IF EXISTS MEStemp.AssetLevel;

Create External table IF NOT EXISTS MEStemp.AssetLevel (AssetLevelID int NOT NULL, AssetID int NOT NULL, SubAssetID int NOT NULL, Sequence varchar(10) NOT NULL, Comments nvarchar(508), FromDT datetime NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/AssetLevel/';

DROP TABLE IF EXISTS MEStemp.Attribute;

Create External table IF NOT EXISTS MEStemp.Attribute (AttributeID int NOT NULL, AttributeTypeID smallint NOT NULL, ProductID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Attribute/';

DROP TABLE IF EXISTS MEStemp.AttributeType;

Create External table IF NOT EXISTS MEStemp.AttributeType (AttributeTypeID smallint NOT NULL, ProductTypeID smallint NOT NULL, Name nvarchar(508) NOT NULL, Description nvarchar(508), ImageURI nvarchar(508), RfAttributeClassID tinyint NOT NULL, IsPassFail tinyint NOT NULL, IsRaiseCA tinyint NOT NULL, LikelyCauses nvarchar(508), Impact nvarchar(508), Weighting float NOT NULL, Reference1 nvarchar(508), Reference2 nvarchar(508), Reference3 nvarchar(508), Reference4 nvarchar(508), Reference5 nvarchar(508), AttributeTypeCode nvarchar(100),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/AttributeType/';

DROP TABLE IF EXISTS MEStemp.AttributeTypeGroup;

Create External table IF NOT EXISTS MEStemp.AttributeTypeGroup (AttributeTypeGroupID smallint NOT NULL, Name nvarchar(508) NOT NULL, Description nvarchar(508), AttributeTypeGroupCode nvarchar(100),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/AttributeTypeGroup/';

DROP TABLE IF EXISTS MEStemp.AVAttribute;

Create External table IF NOT EXISTS MEStemp.AVAttribute (AcquisitionVariantID smallint NOT NULL, TargetSampleSize int NOT NULL, RfAttrStudyTypeID tinyint NOT NULL, Instructions nvarchar(-1), ImageURI nvarchar(-1), NoHeads smallint NOT NULL, RfSourceID tinyint NOT NULL, DataDeviceID int NOT NULL, DeviceArgs nvarchar(508), DeviceOutput nvarchar(100),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/AVAttribute/';

DROP TABLE IF EXISTS MEStemp.AVDataEntryOptions;

Create External table IF NOT EXISTS MEStemp.AVDataEntryOptions (AcquisitionVariantID smallint NOT NULL, RfVarGridStyleID smallint NOT NULL, IsPITCols tinyint NOT NULL, IsPISNoCol tinyint NOT NULL, RfAttrGridStyleID smallint NOT NULL, IsStepGrid tinyint NOT NULL, IsAttrSkipStep tinyint NOT NULL, ShowAttrInfoTip tinyint NOT NULL, UseAttrImagePicker tinyint NOT NULL, AttrGridContextMenu tinyint NOT NULL, IsPISerial tinyint NOT NULL, AutoGenerateSerialNo tinyint NOT NULL, PITSampling smallint NOT NULL, MSTSampling smallint NOT NULL, VAInterlace smallint NOT NULL, IsExplicitHS smallint NOT NULL, SampleSizeMode smallint NOT NULL, OnDataCommitGoTo varchar(50) NOT NULL, IsDisableOOSWarnings tinyint NOT NULL, StepEditPolicy smallint NOT NULL, DisableKeyboardOverride tinyint NOT NULL, ChainToStopped smallint NOT NULL, DisplayPlan varchar(3000), AttrPassFailDefault tinyint NOT NULL, VariableSaveMode tinyint NOT NULL, AttrCellClick tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/AVDataEntryOptions/';

DROP TABLE IF EXISTS MEStemp.BOMExplosion;

Create External table IF NOT EXISTS MEStemp.BOMExplosion (PartID int NOT NULL, ParentPartID int NOT NULL, SubPartID int, BOMItemID int NOT NULL, SubPartQuantityTotal float, SubPartUnitID int, RfInvPackTraceabilityID smallint NOT NULL, Lvl int NOT NULL, SortOrder varchar(254) NOT NULL, SubPartQuantity float, BOMVariantID int NOT NULL, Instructions nvarchar(-1), RfBOMItemTypeID tinyint NOT NULL, IsAllowExceedBOMQty tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/BOMExplosion/';

DROP TABLE IF EXISTS MEStemp.BOMItem;

Create External table IF NOT EXISTS MEStemp.BOMItem (BOMItemID int NOT NULL, BOMVariantID int NOT NULL, PartID int NOT NULL, SubPartID int, SubPartQuantity float, SubPartUnitID smallint, RfInvPackTraceabilityID smallint NOT NULL, Sequence varchar(10), Instructions nvarchar(-1), RfBOMItemTypeID tinyint NOT NULL, IsDefault tinyint NOT NULL, IsAllowExceedBOMQty tinyint NOT NULL, Reference1 nvarchar(508), Reference2 nvarchar(508), Reference3 nvarchar(508), Reference4 nvarchar(508), Reference5 nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/BOMItem/';

DROP TABLE IF EXISTS MEStemp.BOMVariant;

Create External table IF NOT EXISTS MEStemp.BOMVariant (BOMVariantID int NOT NULL, BOMVariant nvarchar(508) NOT NULL, PartID int NOT NULL, IsDefault tinyint NOT NULL, BOMVariantIndex int NOT NULL, IsRetired tinyint NOT NULL, LoginID smallint NOT NULL, DT datetime NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/BOMVariant/';

DROP TABLE IF EXISTS MEStemp.BuildRecord;

Create External table IF NOT EXISTS MEStemp.BuildRecord (BuildRecordID int NOT NULL, InvPackCreatedID int NOT NULL, InvPackConsumedID int, QuantityUsed float, UnitID smallint, BuildDT datetime NOT NULL, IsEdited tinyint NOT NULL, RevisionNo smallint NOT NULL, BuildMachineStageID smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/BuildRecord/';

DROP TABLE IF EXISTS MEStemp.BuildRecord_;

Create External table IF NOT EXISTS MEStemp.BuildRecord_ (BuildRecordID int NOT NULL, Comments nvarchar(508), LoginID smallint NOT NULL, OpenedDT datetime NOT NULL, SavedDT datetime NOT NULL, EditedBuildRecordID int, OriginalLoginID smallint NOT NULL, Reference1 nvarchar(508), Reference2 nvarchar(508), Reference3 nvarchar(508), Reference4 nvarchar(508), Reference5 nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/BuildRecord_/';

DROP TABLE IF EXISTS MEStemp.CauseAction;

Create External table IF NOT EXISTS MEStemp.CauseAction (CauseActionID int NOT NULL, MeasurementSetID int, IsAttributes tinyint NOT NULL, IndividualDefectID int, AttributeTypeID smallint, Cause nvarchar(508), Action nvarchar(508), DT datetime, VarChartOCID int, RfIsOSID tinyint, VarMeasurementID int, VariableTypeID int, IsEdited tinyint NOT NULL, RevisionNo smallint NOT NULL, ProductID int, AcquisitionID smallint, MachineStageID smallint, RfAttributeClassID tinyint, HeadNo smallint, SampleNo smallint, PositionNo smallint, RfAcquisitionClassID tinyint, SPCChartComponentID int, SPCRuleID smallint, RfCauseActionStateID tinyint NOT NULL, TAGMSVALUE1 nvarchar(508), TAGMSTYPE1ID smallint, CreateDT datetime, AcknowledgeDT datetime NOT NULL, CloseDT datetime NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/CauseAction/';

DROP TABLE IF EXISTS MEStemp.CauseAction_;

Create External table IF NOT EXISTS MEStemp.CauseAction_ (CauseActionID int NOT NULL, Comments nvarchar(4000), LoginID smallint NOT NULL, OpenedDT datetime NOT NULL, SavedDT datetime NOT NULL, OriginalLoginID smallint NOT NULL, EditedCauseActionID int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/CauseAction_/';

DROP TABLE IF EXISTS MEStemp.Comments;

Create External table IF NOT EXISTS MEStemp.Comments (CommentsID int NOT NULL, LoginID smallint NOT NULL, DT datetime NOT NULL, Subject nvarchar(100) NOT NULL, Comments nvarchar(4000) NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Comments/';

DROP TABLE IF EXISTS MEStemp.CommonFault;

Create External table IF NOT EXISTS MEStemp.CommonFault (CommonFaultID smallint NOT NULL, CommonFault nvarchar(508) NOT NULL, Description nvarchar(508), LineTypeID smallint, MachineStageTypeID smallint, ToolTypeID smallint, GaugeTypeID smallint, PartTypeID smallint, RfAssetTypeID tinyint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/CommonFault/';

DROP TABLE IF EXISTS MEStemp.Concern;

Create External table IF NOT EXISTS MEStemp.Concern (ConcernID int NOT NULL, ConcernStatusID tinyint, ConcernTypeID smallint, RaisedDT datetime, ClosedDT datetime NOT NULL, SeverityID tinyint NOT NULL, DueDate datetime NOT NULL, ConcernOwnerID smallint NOT NULL, DepartmentOwnerID smallint NOT NULL, CustomerID smallint NOT NULL, CustomerContact nvarchar(508), CustomerReference nvarchar(508), AffectOnCustomer nvarchar(508), PartTypeID smallint, PartID int, WOID int, LineID smallint, ProductionFromDT datetime, ShiftID int, QuantityAffected bigint, QuantityUsed bigint, InvIsolatedReasonID int, Cost float, LoginID smallint NOT NULL, ModifiedDT datetime, Concern nvarchar(4000) NOT NULL, SubmitterLoginID smallint NOT NULL, CustomerProduct nvarchar(508), RfCurrencyID tinyint NOT NULL, MachineStageID smallint, Reference1 nvarchar(508), Reference2 nvarchar(508), Reference3 nvarchar(508), RfConcernApplicationTypeID tinyint NOT NULL, HeadNo smallint, ProductionToDT datetime, QuantityFound bigint, QuantityReturned bigint, QuantityScrapped bigint, QuantitySorted bigint, SupplierID smallint, ToolID int, VariableTypeID int, AttributeTypeID smallint, ClosedByLoginID smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Concern/';

DROP TABLE IF EXISTS MEStemp.ConcernStatus;

Create External table IF NOT EXISTS MEStemp.ConcernStatus (ConcernStatusID tinyint NOT NULL, ConcernStatus nvarchar(508) NOT NULL, ConditionalFormat nvarchar(508), IsClosedState tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ConcernStatus/';

DROP TABLE IF EXISTS MEStemp.ConcernType;

Create External table IF NOT EXISTS MEStemp.ConcernType (ConcernTypeID smallint NOT NULL, ConcernType nvarchar(508) NOT NULL, Description nvarchar(508), ConcernTypeCode nvarchar(508), Format nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ConcernType/';

DROP TABLE IF EXISTS MEStemp.CostCentre;

Create External table IF NOT EXISTS MEStemp.CostCentre (CostCentreID smallint NOT NULL, CostCentre nvarchar(100) NOT NULL, Description nvarchar(500),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/CostCentre/';

DROP TABLE IF EXISTS MEStemp.CRN_AcquisitionVariableType;

Create External table IF NOT EXISTS MEStemp.CRN_AcquisitionVariableType (AcquisitionVariableTypeID int NOT NULL, JobCommentLength int, RfTorqueDirectionID smallint NOT NULL, RfCycleEndTimeID tinyint NOT NULL, ThresholdTorque float, RfMeasurementTypeID smallint NOT NULL, RfFrequencyResponseID tinyint NOT NULL, UnitID smallint NOT NULL, AuditAngle smallint, CrnAdapterID smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/CRN_AcquisitionVariableType/';

DROP TABLE IF EXISTS MEStemp.CrnAdapter;

Create External table IF NOT EXISTS MEStemp.CrnAdapter (CrnAdapterID smallint NOT NULL, Length tinyint NOT NULL, ModifiedDT datetime NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/CrnAdapter/';

DROP TABLE IF EXISTS MEStemp.Customer;

Create External table IF NOT EXISTS MEStemp.Customer (CustomerID smallint NOT NULL, Customer nvarchar(508) NOT NULL, CustomerCode nvarchar(508), AddressID smallint NOT NULL, RfLanguageID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Customer/';

DROP TABLE IF EXISTS MEStemp.DefectCount;

Create External table IF NOT EXISTS MEStemp.DefectCount (DefectCountID int NOT NULL, MeasurementSetID int NOT NULL, HeadNo smallint NOT NULL, DefectCount int NOT NULL, Ignored tinyint NOT NULL, AttributeTypeID smallint, ProductID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/DefectCount/';

DROP TABLE IF EXISTS MEStemp.DefectCountP;

Create External table IF NOT EXISTS MEStemp.DefectCountP (DefectCountPID int NOT NULL, MeasurementSetPID int NOT NULL, HeadNo smallint NOT NULL, DefectCount int NOT NULL, Ignored tinyint NOT NULL, AttributeTypeID smallint, ProductID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/DefectCountP/';

DROP TABLE IF EXISTS MEStemp.Department;

Create External table IF NOT EXISTS MEStemp.Department (DepartmentID smallint NOT NULL, Name nvarchar(508) NOT NULL, DepartmentGroup nvarchar(508), Classification1 nvarchar(100), PlantID smallint NOT NULL, DepartmentCode nvarchar(100), Description nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Department/';

DROP TABLE IF EXISTS MEStemp.Document;

Create External table IF NOT EXISTS MEStemp.Document (DocumentID int NOT NULL, FileName nvarchar(2048) NOT NULL, Description nvarchar(2048), MimeType nvarchar(100) NOT NULL, Attachment varbinary(-1), UploadDT datetime NOT NULL, LoginID int NOT NULL, DocumentTypeID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Document/';

DROP TABLE IF EXISTS MEStemp.DocumentType;

Create External table IF NOT EXISTS MEStemp.DocumentType (DocumentTypeID smallint NOT NULL, DocumentType nvarchar(508) NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/DocumentType/';

DROP TABLE IF EXISTS MEStemp.dtproperties;

Create External table IF NOT EXISTS MEStemp.dtproperties (id int NOT NULL, objectid int, property varchar(64) NOT NULL, value varchar(255), uvalue nvarchar(510), lvalue int, version int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/dtproperties/';

DROP TABLE IF EXISTS MEStemp.EBATrigger;

Create External table IF NOT EXISTS MEStemp.EBATrigger (EBATriggerID int NOT NULL, RfTriggerTypeID smallint NOT NULL, ContextDictionary nvarchar(2048), TriggerOwnerID int NOT NULL, CreateDT datetime NOT NULL, ProcessedDT datetime NOT NULL, RfWatchProcessStateID smallint NOT NULL, Message nvarchar(7000), TotalTime float, OracleInsertTime float, OracleConnectionTime float, OracleInsertStatement nvarchar(-1),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/EBATrigger/';

DROP TABLE IF EXISTS MEStemp.ELData_Acq;

Create External table IF NOT EXISTS MEStemp.ELData_Acq (MeasurementSetID int NOT NULL, SerialNo nvarchar(508) NOT NULL, BINNo nvarchar(508) NOT NULL, Komax1 nvarchar(508), CktFormBuildMachine nvarchar(508), ElBuildMachine nvarchar(508), Comments nvarchar(4000), DT datetime2(3) NOT NULL, NumCells int, CellHole int, LiftedTab int, MultipleCrack int, DiagonalCrack int, ReversePolarity int, ShatteredCell int, ShortedPasteBusbar int, ShortedPasteMiddle int, ShortedPasteUnknown int, VerticalCrack int, VisibleCrackFab int, VisibleCrackMod int, Weird int, CellReplacementFab int, CellReplacementMod int, TouchUpthenGood int, WipethenGood int, Rework int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ELData_Acq/';

DROP TABLE IF EXISTS MEStemp.ELData_Acq_Buffer;

Create External table IF NOT EXISTS MEStemp.ELData_Acq_Buffer (MeasurementSetID int NOT NULL, SerialNo nvarchar(508) NOT NULL, BINNo nvarchar(508) NOT NULL, Komax1 nvarchar(508), CktFormBuildMachine nvarchar(508), ElBuildMachine nvarchar(508), Comments nvarchar(4000), DT datetime2(3) NOT NULL, NumCells int, CellHole int, LiftedTab int, MultipleCrack int, DiagonalCrack int, ReversePolarity int, ShatteredCell int, ShortedPasteBusbar int, ShortedPasteMiddle int, ShortedPasteUnknown int, VerticalCrack int, VisibleCrackFab int, VisibleCrackMod int, Weird int, CellReplacementFab int, CellReplacementMod int, TouchUpthenGood int, WipethenGood int, Rework int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ELData_Acq_Buffer/';

DROP TABLE IF EXISTS MEStemp.ELData_CellInfo;

Create External table IF NOT EXISTS MEStemp.ELData_CellInfo (ConsumesCells nvarchar(508) NOT NULL, ModuleID nvarchar(508) NOT NULL, BINNo nvarchar(508) NOT NULL, Variant1 nvarchar(200), CktFormBuildDT datetime2(3) NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ELData_CellInfo/';

DROP TABLE IF EXISTS MEStemp.ELData_CellInfo_Buffer;

Create External table IF NOT EXISTS MEStemp.ELData_CellInfo_Buffer (ConsumesCells nvarchar(508) NOT NULL, ModuleID nvarchar(508) NOT NULL, BINNo nvarchar(508) NOT NULL, Variant1 nvarchar(200), CktFormBuildDT datetime2(3) NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ELData_CellInfo_Buffer/';

DROP TABLE IF EXISTS MEStemp.Fault;

Create External table IF NOT EXISTS MEStemp.Fault (FaultID int NOT NULL, Fault nvarchar(508) NOT NULL, FaultComments nvarchar(508), AssetID int NOT NULL, MaintenanceWorkID int NOT NULL, ReportedByLoginID smallint NOT NULL, FaultReportedDT datetime NOT NULL, CommonFaultID smallint NOT NULL, FaultStateID int, TimeToRespond float, TimeToRepair float, TimeResponseToRepair float, RevisionNo smallint NOT NULL, IsEdited tinyint NOT NULL, LoginID smallint NOT NULL, ModifiedDT datetime NOT NULL, Cause nvarchar(1000), MachineStopID int, Symptoms nvarchar(1000),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Fault/';

DROP TABLE IF EXISTS MEStemp.FaultState;

Create External table IF NOT EXISTS MEStemp.FaultState (FaultStateID int NOT NULL, FaultID int NOT NULL, FromDT datetime NOT NULL, ToDT datetime NOT NULL, FaultStatusID smallint NOT NULL, Comments nvarchar(508), LoginID smallint NOT NULL, OpenedDT datetime NOT NULL, SavedDT datetime NOT NULL, IsEdited tinyint NOT NULL, RevisionNo tinyint NOT NULL, OriginalLoginID smallint, EditedFaultStateID int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/FaultState/';

DROP TABLE IF EXISTS MEStemp.FaultStatus;

Create External table IF NOT EXISTS MEStemp.FaultStatus (FaultStatusID smallint NOT NULL, FaultStatus nvarchar(508) NOT NULL, Description nvarchar(508), RfFaultStatusID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/FaultStatus/';

DROP TABLE IF EXISTS MEStemp.Gauge;

Create External table IF NOT EXISTS MEStemp.Gauge (GaugeID int NOT NULL, Gauge nvarchar(508) NOT NULL, GaugeTypeID smallint NOT NULL, DataDeviceID smallint, SerialNo nvarchar(508), SupplierID smallint, DatePurchased datetime,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Gauge/';

DROP TABLE IF EXISTS MEStemp.GaugeType;

Create External table IF NOT EXISTS MEStemp.GaugeType (GaugeTypeID smallint NOT NULL, GaugeType nvarchar(508) NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/GaugeType/';

DROP TABLE IF EXISTS MEStemp.IndividualDefect;

Create External table IF NOT EXISTS MEStemp.IndividualDefect (IndividualDefectID int NOT NULL, PartInstanceID int NOT NULL, Ignored tinyint NOT NULL, DefectCount int NOT NULL, MeasurementSetID int NOT NULL, AttributeTypeID smallint NOT NULL, ProductID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/IndividualDefect/';

DROP TABLE IF EXISTS MEStemp.InvChangeState;

Create External table IF NOT EXISTS MEStemp.InvChangeState (InvChangeStateID int NOT NULL, InventoryPackID int NOT NULL, FromDT datetime NOT NULL, ToDT datetime NOT NULL, InvIsolatedReasonID smallint, Comments nvarchar(508), LoginID smallint NOT NULL, OpenedDT datetime NOT NULL, SavedDT datetime NOT NULL, IsEdited tinyint NOT NULL, RevisionNo tinyint NOT NULL, OriginalLoginID smallint NOT NULL, EditedInvChangeStateID int, InvStateID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/InvChangeState/';

DROP TABLE IF EXISTS MEStemp.InventoryAtLocation;

Create External table IF NOT EXISTS MEStemp.InventoryAtLocation (InventoryAtLocationID int NOT NULL, LocationID int NOT NULL, InventoryPackID int NOT NULL, FromDT datetime NOT NULL, ToDT datetime NOT NULL, IsEdited tinyint NOT NULL, RevisionNo tinyint NOT NULL, EditedInvAtLocationID int, MovedByLoginID smallint NOT NULL, ModifiedDT datetime NOT NULL, Reference1 nvarchar(508), Reference2 nvarchar(508), Reference3 nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/InventoryAtLocation/';

DROP TABLE IF EXISTS MEStemp.InventoryLabel;

Create External table IF NOT EXISTS MEStemp.InventoryLabel (InventoryLabelID int NOT NULL, MachineStage nvarchar(508), PartNumber nvarchar(508), PartType nvarchar(508), QuantityOrdered float, WONumber nvarchar(508), InventoryPackNo nvarchar(508), CreatedFromDT datetime, Quantity float, Unit nvarchar(40), Line nvarchar(508), LabelTemplate nvarchar(508), LabelsPerInventoryPack smallint, ItemsPerLayer float, LayersPerInventoryPack int, PrinterURL nvarchar(2048) NOT NULL, PrintedDT datetime NOT NULL, ItemsPerInventoryPack float, ScannedDT datetime NOT NULL, IsCancelled tinyint NOT NULL, OrderQuantityPacks int, LineShortName nvarchar(100), PackNo int, IsReprint tinyint NOT NULL, PartDescription nvarchar(508), LabelSaveDirectory nvarchar(508), JobID int, IsPrintLandscape tinyint NOT NULL, NoCopies smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/InventoryLabel/';

DROP TABLE IF EXISTS MEStemp.InventoryLabel_;

Create External table IF NOT EXISTS MEStemp.InventoryLabel_ (InventoryLabelID int NOT NULL, LabelColour nvarchar(50), EANCode varchar(20), CusNum int, PackingCode nvarchar(100), CustomerPartNo nvarchar(508), CustomerPartDescription nvarchar(508), LabelText1 nvarchar(508), LabelText2 nvarchar(508), LabelText3 nvarchar(508), Customer nvarchar(508), Address1 nvarchar(508), Address2 nvarchar(508), City nvarchar(50), County nvarchar(50), PostCode nvarchar(50), Country nvarchar(50),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/InventoryLabel_/';

DROP TABLE IF EXISTS MEStemp.InventoryLabel_custom;

Create External table IF NOT EXISTS MEStemp.InventoryLabel_custom (InventoryLabelID int NOT NULL, InventoryPackID int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/InventoryLabel_custom/';

DROP TABLE IF EXISTS MEStemp.InventoryPack;

Create External table IF NOT EXISTS MEStemp.InventoryPack (InventoryPackID int NOT NULL, InventoryPackNo nvarchar(508) NOT NULL, WOID int NOT NULL, JobID int, CreatedFromDT datetime NOT NULL, CreatedToDT datetime NOT NULL, CreatedOnMachineStageID smallint, IsEdited tinyint NOT NULL, RevisionNo tinyint NOT NULL, PartID int NOT NULL, Quantity float NOT NULL, UnitID smallint NOT NULL, Ignored tinyint NOT NULL, InventoryPackIndex int, CustomerUniqueKey nvarchar(508) NOT NULL, ReceivedDT datetime, BirthDT datetime, LastBuildRecordID int, msrepl_tran_version uniqueidentifier NOT NULL, CurrentQuantity float, QuantityAdjustment float, PositionNumber tinyint NOT NULL, LocationID int NOT NULL, InvStateID int NOT NULL, LocationFromDT datetime,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/InventoryPack/';

DROP TABLE IF EXISTS MEStemp.InventoryPack_;

Create External table IF NOT EXISTS MEStemp.InventoryPack_ (InventoryPackID int NOT NULL, Comments nvarchar(508), LoginID smallint NOT NULL, OpenedDT datetime NOT NULL, SavedDT datetime NOT NULL, OriginalLoginID smallint NOT NULL, EditedInventoryPackID int, Reference1 nvarchar(508), Reference2 nvarchar(508), Reference3 nvarchar(508), Reference4 nvarchar(508), Reference5 nvarchar(508), Reference6 nvarchar(508), Reference7 nvarchar(508), Reference8 nvarchar(508), Reference9 nvarchar(508), Reference10 nvarchar(508), SupplierID smallint NOT NULL, SupplierReference nvarchar(508), UseByDT datetime NOT NULL, UseAfterDT datetime NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/InventoryPack_/';

DROP TABLE IF EXISTS MEStemp.InventoryPack_Custom;

Create External table IF NOT EXISTS MEStemp.InventoryPack_Custom (InventoryPackId int NOT NULL, Txn_date datetime, OracleStatus tinyint NOT NULL, IntendedWOID int, FinalWOID int, OriginalQty float, Seq_Id int, From_Subinventory nchar(20), From_Locator nchar(80), To_Subinventory nchar(20), To_Locator nchar(80), ScrappedShiftID int, LastInventoryLabelID int, ScrapLocationID int, MachineLocationID int, ScrapReasonID smallint, ModuleInfoID int NOT NULL, ResealedDT datetime, CrateID bigint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/InventoryPack_Custom/';

DROP TABLE IF EXISTS MEStemp.InventoryUsage;

Create External table IF NOT EXISTS MEStemp.InventoryUsage (InventoryUsageID int NOT NULL, JobID int NOT NULL, MachineStageID smallint NOT NULL, InventoryPackID int NOT NULL, FromDT datetime NOT NULL, ToDT datetime NOT NULL, IsEdited tinyint NOT NULL, RevisionNo tinyint NOT NULL, IsReintroduced tinyint NOT NULL, Quantity float,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/InventoryUsage/';

DROP TABLE IF EXISTS MEStemp.InventoryUsage_;

Create External table IF NOT EXISTS MEStemp.InventoryUsage_ (InventoryUsageID int NOT NULL, StartUsingComments nvarchar(508), StopUsingComments nvarchar(508), StartLoginID smallint NOT NULL, StopLoginID smallint NOT NULL, OpenedStartDT datetime NOT NULL, OpenedEndDT datetime NOT NULL, SavedStartDT datetime NOT NULL, SavedEndDT datetime NOT NULL, OriginalLoginID smallint NOT NULL, EditedInventoryUsageID int, Reference1 nvarchar(508), Reference2 nvarchar(508), Reference3 nvarchar(508), Reference4 nvarchar(508), Reference5 nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/InventoryUsage_/';

DROP TABLE IF EXISTS MEStemp.InvIsolatedReason;

Create External table IF NOT EXISTS MEStemp.InvIsolatedReason (InvIsolatedReasonID smallint NOT NULL, PartTypeID smallint NOT NULL, IsolatedReason nvarchar(508) NOT NULL, InvStateID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/InvIsolatedReason/';

DROP TABLE IF EXISTS MEStemp.InvState;

Create External table IF NOT EXISTS MEStemp.InvState (InvStateID smallint NOT NULL, InvState nvarchar(508) NOT NULL, Description nvarchar(508), RfInvStateID smallint NOT NULL, InvStateCode nvarchar(508), IsAddToProduction tinyint NOT NULL, IsAddToHFI tinyint NOT NULL, IsDefault tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/InvState/';

DROP TABLE IF EXISTS MEStemp.Job;

Create External table IF NOT EXISTS MEStemp.Job (JobID int NOT NULL, WOID int NOT NULL, RfJobStatusID tinyint NOT NULL, DeadlineDT datetime NOT NULL, ActualStartDT datetime NOT NULL, ActualEndDT datetime NOT NULL, PlannedStartDT datetime NOT NULL, PlannedEndDT datetime NOT NULL, PlannedDuration float NOT NULL, EstimatedStartDT datetime NOT NULL, EstimatedEndDT datetime NOT NULL, EstimatedDuration float NOT NULL, ScheduledSequence smallint NOT NULL, LineID smallint NOT NULL, ProductionStartDT datetime NOT NULL, ProductionEndDT datetime NOT NULL, TheoreticalCycleTime float, BudgetChangeOver int NOT NULL, QuantityOrdered float NOT NULL, IsCalculated tinyint NOT NULL, LineGroupID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Job/';

DROP TABLE IF EXISTS MEStemp.Job_;

Create External table IF NOT EXISTS MEStemp.Job_ (JobID int NOT NULL, JobInstructions nvarchar(2048), ReleaseDT datetime NOT NULL, ReleaseLoginID smallint NOT NULL, StartLoginID smallint NOT NULL, EndLoginID smallint NOT NULL, Comments nvarchar(2000), ModifiedDt datetime NOT NULL, ModifiedLoginID smallint NOT NULL, StoppedReason nvarchar(508), SkippedReason nvarchar(508), SkippedDT datetime NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Job_/';

DROP TABLE IF EXISTS MEStemp.Job_Custom;

Create External table IF NOT EXISTS MEStemp.Job_Custom (JobID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Job_Custom/';

DROP TABLE IF EXISTS MEStemp.Line;

Create External table IF NOT EXISTS MEStemp.Line (LineID smallint NOT NULL, Line nvarchar(508) NOT NULL, Description nvarchar(508), LineTypeID smallint NOT NULL, IsRetired tinyint NOT NULL, RfIntervalModeID tinyint NOT NULL, IsOEEInventoryIsolation tinyint NOT NULL, IsOEEInventoryReintroduction tinyint NOT NULL, IsCountOutputInventory tinyint NOT NULL, IsCountInputInventory tinyint NOT NULL, IsLineInfoSweepEnabled tinyint NOT NULL, IsWOOnSingleJob tinyint NOT NULL, LineShortName nvarchar(100), CurrentLineInfoID int, LastLineInfoID int, RfInvCreatedDateModeID tinyint NOT NULL, RfEndJobInvModeID tinyint NOT NULL, CurrentJobID int, IsScrapAsQualityLoss tinyint NOT NULL, IsScrapInProductionCounts tinyint NOT NULL, LineDownCostPerHour float NOT NULL, IsContributeLineGroupOutput tinyint NOT NULL, IsContributeLineGroupWaste tinyint NOT NULL, IsContributeLineGroupDowntime tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Line/';

DROP TABLE IF EXISTS MEStemp.Line_;

Create External table IF NOT EXISTS MEStemp.Line_ (LineID smallint NOT NULL, OEETarget float, WasteTarget float, Reference1 nvarchar(508), Reference2 nvarchar(508), Reference3 nvarchar(508), Reference4 nvarchar(508), Reference5 nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Line_/';

DROP TABLE IF EXISTS MEStemp.Line_Custom;

Create External table IF NOT EXISTS MEStemp.Line_Custom (LineID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Line_Custom/';

DROP TABLE IF EXISTS MEStemp.LineCell;

Create External table IF NOT EXISTS MEStemp.LineCell (LineCellID smallint NOT NULL, LineID smallint NOT NULL, IsPartNumber tinyint NOT NULL, IsWONumber tinyint NOT NULL, IsCustomer tinyint NOT NULL, IsDescription tinyint NOT NULL, IsHeader tinyint NOT NULL, IsRows tinyint NOT NULL, ConfigLayout varchar(50) NOT NULL, IsClickable tinyint NOT NULL, IsStopJob tinyint NOT NULL, IsStartJob tinyint NOT NULL, IsEditable tinyint NOT NULL, IsStartNextJob tinyint NOT NULL, IsWORef1 tinyint NOT NULL, IsWORef2 tinyint NOT NULL, IsWORef3 tinyint NOT NULL, IsWorkInstruction tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LineCell/';

DROP TABLE IF EXISTS MEStemp.LineGroup;

Create External table IF NOT EXISTS MEStemp.LineGroup (LineGroupID smallint NOT NULL, LineGroup nvarchar(100) NOT NULL, LineGroupCode nvarchar(100), Description nvarchar(508), OEETarget float, WasteTarget float, Reference1 nvarchar(508), Reference2 nvarchar(508), Reference3 nvarchar(508), Reference4 nvarchar(508), Reference5 nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LineGroup/';

DROP TABLE IF EXISTS MEStemp.LineInfo;

Create External table IF NOT EXISTS MEStemp.LineInfo (LineInfoID int NOT NULL, RfLineInfoEventID tinyint NOT NULL, LineID smallint NOT NULL, ShiftID int, WOID int, FromDT datetime, ToDT datetime, rfDayHourPeriodID int NOT NULL, TotalTime int, ExcludedTime int, ScheduledTime int, STOperations int, STInduced int, DTTechnical int, DTOperations int, DTQuality int, DownTime int, StopTime int, RunTime int, NotRunTime int, TheoreticalCycleTime float, QuantityScrap float, QuantityGood float, ModifiedDT datetime NOT NULL, LoginID smallint NOT NULL, QuantityIn float, TotalInventoryProduced float, TotalInventoryReintroduced float, NetQuantityProduced float, QuantityInLineSpoilage float, TotalSpoilage float, AccountedSpoilage float, TotalInventoryIsolated float, ProductionStopJobID int, TotalUnitsProduced float, BottleNeckMachineStageID smallint, InventoryIn int, InterProdStopTime int, InProdStopTime int, QuantityUnitsIn float, QuantityInventoryIn float, TotalQuantityProduced float, TotalPacksProduced int, TotalPacksIsolated int, TotalPacksReintroduced int, TotalPacksMade int, ValidState tinyint NOT NULL, LineGroupID smallint, LineGroupShiftID int, LineGroupBottleneckID smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LineInfo/';

DROP TABLE IF EXISTS MEStemp.LineInfo_custom;

Create External table IF NOT EXISTS MEStemp.LineInfo_custom (LineInfoID int NOT NULL, CellsProduced decimal(8,0) NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LineInfo_custom/';

DROP TABLE IF EXISTS MEStemp.LineInfoRecalc;

Create External table IF NOT EXISTS MEStemp.LineInfoRecalc (LineInfoID int NOT NULL, ModifiedDT datetime NOT NULL, IsOEEInventoryIsolation tinyint, IsOEEInventoryReintroduction tinyint, IsOEESpoilage tinyint, IsOEEDowntime tinyint, IsOEEInventoryProduction tinyint, NoBottlenecks tinyint, BottleneckMachineStageID smallint, IsCountOutputInventory tinyint, IsCountInputInventory tinyint, IsOEEProductionUnits tinyint, LoginID smallint NOT NULL, SessionID int, PlantID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LineInfoRecalc/';

DROP TABLE IF EXISTS MEStemp.LineSection;

Create External table IF NOT EXISTS MEStemp.LineSection (LineSectionID smallint NOT NULL, LineSection nvarchar(508) NOT NULL, Description nvarchar(508), IsRetired tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LineSection/';

DROP TABLE IF EXISTS MEStemp.LineType;

Create External table IF NOT EXISTS MEStemp.LineType (LineTypeID smallint NOT NULL, LineType nvarchar(508) NOT NULL, Description nvarchar(508), IsRetired tinyint NOT NULL, LineInfoPrecedence tinyint NOT NULL, BudgetChangeOver int NOT NULL, BudgetEfficiency float NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LineType/';

DROP TABLE IF EXISTS MEStemp.LkACAChain;

Create External table IF NOT EXISTS MEStemp.LkACAChain (AcquisitionChainID smallint NOT NULL, AcquisitionContextID int NOT NULL, Sequence tinyint NOT NULL, LkACAChainID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkACAChain/';

DROP TABLE IF EXISTS MEStemp.LkAGAV;

Create External table IF NOT EXISTS MEStemp.LkAGAV (AcquisitionGroupID smallint NOT NULL, AcquisitionVariantID smallint NOT NULL, Sequence smallint NOT NULL, LkAGAVID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkAGAV/';

DROP TABLE IF EXISTS MEStemp.LkATATG;

Create External table IF NOT EXISTS MEStemp.LkATATG (AttributeTypeGroupID smallint NOT NULL, AttributeTypeID smallint NOT NULL, LkATATGID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkATATG/';

DROP TABLE IF EXISTS MEStemp.LkCDefInCView;

Create External table IF NOT EXISTS MEStemp.LkCDefInCView (SPCChartDefID int NOT NULL, SPCChartSetViewID int NOT NULL, Sequence smallint NOT NULL, LkCDefInCViewID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkCDefInCView/';

DROP TABLE IF EXISTS MEStemp.LkConcernAction;

Create External table IF NOT EXISTS MEStemp.LkConcernAction (LKConcernActionID int NOT NULL, ConcernID int NOT NULL, ActionID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkConcernAction/';

DROP TABLE IF EXISTS MEStemp.LkConcernDocument;

Create External table IF NOT EXISTS MEStemp.LkConcernDocument (ConcernID int NOT NULL, DocumentID int NOT NULL, LKConcernDocumentID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkConcernDocument/';

DROP TABLE IF EXISTS MEStemp.LKConcernInventoryPack;

Create External table IF NOT EXISTS MEStemp.LKConcernInventoryPack (LKConcernInventoryPackID int NOT NULL, ConcernID int NOT NULL, InventoryPackID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LKConcernInventoryPack/';

DROP TABLE IF EXISTS MEStemp.LkConcernRootCause;

Create External table IF NOT EXISTS MEStemp.LkConcernRootCause (LkConcernRootCauseID int NOT NULL, RootCauseID int NOT NULL, ConcernID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkConcernRootCause/';

DROP TABLE IF EXISTS MEStemp.LKCSetCDef;

Create External table IF NOT EXISTS MEStemp.LKCSetCDef (SPCChartSetID smallint NOT NULL, SPCChartDefID int NOT NULL, Sequence smallint NOT NULL, LkCSetCDefID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LKCSetCDef/';

DROP TABLE IF EXISTS MEStemp.LkCutBySPCRule;

Create External table IF NOT EXISTS MEStemp.LkCutBySPCRule (SPCRuleID smallint NOT NULL, RfCutByID smallint NOT NULL, LkCutBySPCRuleID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkCutBySPCRule/';

DROP TABLE IF EXISTS MEStemp.LkDocumentAsset;

Create External table IF NOT EXISTS MEStemp.LkDocumentAsset (LkDocumentAssetID int NOT NULL, DocumentID int NOT NULL, AssetID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkDocumentAsset/';

DROP TABLE IF EXISTS MEStemp.LkDocumentInvPack;

Create External table IF NOT EXISTS MEStemp.LkDocumentInvPack (LkDocumentInvPackID int NOT NULL, DocumentID int NOT NULL, InventoryPackID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkDocumentInvPack/';

DROP TABLE IF EXISTS MEStemp.LkDocumentMaintProc;

Create External table IF NOT EXISTS MEStemp.LkDocumentMaintProc (LkDocumentMaintProcID int NOT NULL, DocumentID int NOT NULL, MaintenanceProcedureID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkDocumentMaintProc/';

DROP TABLE IF EXISTS MEStemp.LkDocumentWIProc;

Create External table IF NOT EXISTS MEStemp.LkDocumentWIProc (LkDocumentWIProcID int NOT NULL, WorkInstructionProcedureID smallint NOT NULL, DocumentID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkDocumentWIProc/';

DROP TABLE IF EXISTS MEStemp.LkDocumentWIStep;

Create External table IF NOT EXISTS MEStemp.LkDocumentWIStep (LkDocumentWIStepID int NOT NULL, WorkInstructionStepID int NOT NULL, DocumentID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkDocumentWIStep/';

DROP TABLE IF EXISTS MEStemp.LkFaultMaintWork;

Create External table IF NOT EXISTS MEStemp.LkFaultMaintWork (MaintenanceWorkID int NOT NULL, FaultID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkFaultMaintWork/';

DROP TABLE IF EXISTS MEStemp.LkInvPackInvPack;

Create External table IF NOT EXISTS MEStemp.LkInvPackInvPack (LkInvPackInvPackID int NOT NULL, InventoryPackFromID int NOT NULL, InventoryPackToID int NOT NULL, RfInventoryLinkTypeID smallint NOT NULL, LoginID smallint NOT NULL, ModifiedDT datetime NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkInvPackInvPack/';

DROP TABLE IF EXISTS MEStemp.LkLineLineGroup;

Create External table IF NOT EXISTS MEStemp.LkLineLineGroup (LkLineLineGroupID int NOT NULL, LineGroupID smallint NOT NULL, LineID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkLineLineGroup/';

DROP TABLE IF EXISTS MEStemp.LkLLS;

Create External table IF NOT EXISTS MEStemp.LkLLS (LineID smallint NOT NULL, LineSectionID smallint NOT NULL, LkLLSID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkLLS/';

DROP TABLE IF EXISTS MEStemp.LkLocationMachineStage;

Create External table IF NOT EXISTS MEStemp.LkLocationMachineStage (LkLocationMachineStageId int NOT NULL, MachineStageId smallint NOT NULL, LocationId int NOT NULL, IsLocationIn tinyint NOT NULL, IsLocationOut tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkLocationMachineStage/';

DROP TABLE IF EXISTS MEStemp.LkMachineStagePrinter;

Create External table IF NOT EXISTS MEStemp.LkMachineStagePrinter (LkMachineStagePrinterID smallint NOT NULL, MachineStageID smallint NOT NULL, PrinterID tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkMachineStagePrinter/';

DROP TABLE IF EXISTS MEStemp.LkMaintProcPersonnel;

Create External table IF NOT EXISTS MEStemp.LkMaintProcPersonnel (LkMaintProcPersonnelID int NOT NULL, MaintenanceProcedureID int NOT NULL, PersonnelID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkMaintProcPersonnel/';

DROP TABLE IF EXISTS MEStemp.LkMGMS;

Create External table IF NOT EXISTS MEStemp.LkMGMS (MachineStageID smallint NOT NULL, MachineGroupID smallint NOT NULL, LkMGMSID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkMGMS/';

DROP TABLE IF EXISTS MEStemp.LkMPGMP;

Create External table IF NOT EXISTS MEStemp.LkMPGMP (LkMPGMPID int NOT NULL, MaintenanceProcedureGroupID int NOT NULL, MaintenanceProcedureID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkMPGMP/';

DROP TABLE IF EXISTS MEStemp.LkMSRGMSR;

Create External table IF NOT EXISTS MEStemp.LkMSRGMSR (MachineStopReasonGroupID smallint NOT NULL, MachineStopReasonID smallint NOT NULL, LkMSRGMSRID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkMSRGMSR/';

DROP TABLE IF EXISTS MEStemp.LkOpTMST;

Create External table IF NOT EXISTS MEStemp.LkOpTMST (OperationTypeID smallint NOT NULL, MachineStageTypeID smallint NOT NULL, LkOpTMSTID int NOT NULL, PassName nvarchar(100) NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkOpTMST/';

DROP TABLE IF EXISTS MEStemp.LkOpTPartTConsumed;

Create External table IF NOT EXISTS MEStemp.LkOpTPartTConsumed (PartTypeID smallint NOT NULL, OperationTypeID smallint NOT NULL, IsInventoryCarriedOver tinyint NOT NULL, LkOpTPartTConsumedID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkOpTPartTConsumed/';

DROP TABLE IF EXISTS MEStemp.LkPartAlternatePart;

Create External table IF NOT EXISTS MEStemp.LkPartAlternatePart (LkPartAlternatePartID int NOT NULL, AlternatePartID int NOT NULL, PartID int NOT NULL, PartToAlternatePartRatio float NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkPartAlternatePart/';

DROP TABLE IF EXISTS MEStemp.LkPartDocument;

Create External table IF NOT EXISTS MEStemp.LkPartDocument (LkPartDocumentID int NOT NULL, PartID int NOT NULL, DocumentID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkPartDocument/';

DROP TABLE IF EXISTS MEStemp.LkPartLineSection;

Create External table IF NOT EXISTS MEStemp.LkPartLineSection (PartID int NOT NULL, LineSectionID smallint NOT NULL, LkPartLineSectionID int NOT NULL, TheoreticalCycleTime float NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkPartLineSection/';

DROP TABLE IF EXISTS MEStemp.LkPartSupplier;

Create External table IF NOT EXISTS MEStemp.LkPartSupplier (LkPartSupplierID int NOT NULL, PartID int NOT NULL, SupplierID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkPartSupplier/';

DROP TABLE IF EXISTS MEStemp.LKPartUnit;

Create External table IF NOT EXISTS MEStemp.LKPartUnit (PartID int NOT NULL, UnitID smallint NOT NULL, LkPartUnitID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LKPartUnit/';

DROP TABLE IF EXISTS MEStemp.LkRootCauseAction;

Create External table IF NOT EXISTS MEStemp.LkRootCauseAction (LkRootCauseActionID int NOT NULL, RootCauseID int NOT NULL, ActionID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkRootCauseAction/';

DROP TABLE IF EXISTS MEStemp.LKShiftMachineStage;

Create External table IF NOT EXISTS MEStemp.LKShiftMachineStage (LkShiftMachineStageID int NOT NULL, MachineStageID smallint NOT NULL, ShiftID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LKShiftMachineStage/';

DROP TABLE IF EXISTS MEStemp.LkSPCCSVR;

Create External table IF NOT EXISTS MEStemp.LkSPCCSVR (SPCCalcStyleID smallint NOT NULL, RfViolationRuleID smallint NOT NULL, RfChartComponentID tinyint NOT NULL, LkSPCCSVRID int NOT NULL, RuleRange smallint, RuleGroup smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkSPCCSVR/';

DROP TABLE IF EXISTS MEStemp.LKSPCStatsStyleRfSPCStat_CUS;

Create External table IF NOT EXISTS MEStemp.LKSPCStatsStyleRfSPCStat_CUS (RfSPCStatID smallint NOT NULL, SPCStatsStyleID smallint NOT NULL, LkSPCStatsStyleRfSPCStatID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LKSPCStatsStyleRfSPCStat_CUS/';

DROP TABLE IF EXISTS MEStemp.LkSRGSR;

Create External table IF NOT EXISTS MEStemp.LkSRGSR (LkSRGSRID smallint NOT NULL, ScrapReasonGroupID smallint NOT NULL, ScrapReasonID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkSRGSR/';

DROP TABLE IF EXISTS MEStemp.LkToolTypeOpType;

Create External table IF NOT EXISTS MEStemp.LkToolTypeOpType (ToolTypeID smallint NOT NULL, OperationTypeID smallint NOT NULL, MaxNoTools smallint NOT NULL, LkToolTypeOpTypeID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkToolTypeOpType/';

DROP TABLE IF EXISTS MEStemp.LkTSTC;

Create External table IF NOT EXISTS MEStemp.LkTSTC (ToolStateID int NOT NULL, ToolCavityID int NOT NULL, RfToolCavityStatusID tinyint NOT NULL, ToolCavityStateReasonID smallint NOT NULL, IsCavityChangeState tinyint NOT NULL, LkTSTCID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkTSTC/';

DROP TABLE IF EXISTS MEStemp.LkTTG;

Create External table IF NOT EXISTS MEStemp.LkTTG (ToolGroupID smallint NOT NULL, ToolID int NOT NULL, LkTTGID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkTTG/';

DROP TABLE IF EXISTS MEStemp.LkUCTGUCT;

Create External table IF NOT EXISTS MEStemp.LkUCTGUCT (LkUCTGUCTID smallint NOT NULL, UnitCountTypeGroupID smallint NOT NULL, UnitCountTypeID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkUCTGUCT/';

DROP TABLE IF EXISTS MEStemp.LkVCOCVRule;

Create External table IF NOT EXISTS MEStemp.LkVCOCVRule (VarChartOCID int NOT NULL, RfViolationRuleID smallint NOT NULL, LkVCOCVRuleID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkVCOCVRule/';

DROP TABLE IF EXISTS MEStemp.LkVTVTG;

Create External table IF NOT EXISTS MEStemp.LkVTVTG (VariableTypeGroupID smallint NOT NULL, VariableTypeID int NOT NULL, LkVTVTGID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkVTVTG/';

DROP TABLE IF EXISTS MEStemp.LkWIRBuildRecord;

Create External table IF NOT EXISTS MEStemp.LkWIRBuildRecord (LkWIRBuildRecordID int NOT NULL, WorkInstructionResultID int NOT NULL, BuildRecordID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkWIRBuildRecord/';

DROP TABLE IF EXISTS MEStemp.LkWIRMSetReq;

Create External table IF NOT EXISTS MEStemp.LkWIRMSetReq (LkWIRMSetReqID int NOT NULL, WorkInstructionResultID int NOT NULL, MeasurementSetRequiredID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LkWIRMSetReq/';

DROP TABLE IF EXISTS MEStemp.Location;

Create External table IF NOT EXISTS MEStemp.Location (LocationID int NOT NULL, Location nvarchar(508) NOT NULL, LocationTypeID smallint NOT NULL, IsRetired tinyint NOT NULL, MaxQuantity float NOT NULL, LocationCode nvarchar(100), Description nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Location/';

DROP TABLE IF EXISTS MEStemp.LocationType;

Create External table IF NOT EXISTS MEStemp.LocationType (LocationTypeID smallint NOT NULL, LocationType nvarchar(100) NOT NULL, LocationTypeCode nvarchar(100), Description nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/LocationType/';

DROP TABLE IF EXISTS MEStemp.MachineGroup;

Create External table IF NOT EXISTS MEStemp.MachineGroup (MachineGroupID smallint NOT NULL, MachineGroup nvarchar(508) NOT NULL, Description nvarchar(508), IsRetired tinyint NOT NULL, RfMachineGroupTypeID tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MachineGroup/';

DROP TABLE IF EXISTS MEStemp.MachineStage;

Create External table IF NOT EXISTS MEStemp.MachineStage (MachineStageID smallint NOT NULL, MachineStageTypeID smallint NOT NULL, Name nvarchar(508) NOT NULL, IsRetired tinyint NOT NULL, DepartmentID smallint NOT NULL, Description nvarchar(508), CurrentShiftPatternID smallint NOT NULL, NoHeads smallint NOT NULL, IsOEEBottleNeck tinyint NOT NULL, IsProductionOutput tinyint NOT NULL, MachineStopID int, StoppedScreenThreshold smallint NOT NULL, LineSectionID smallint NOT NULL, IsDiscrete tinyint NOT NULL, RfDowntimeCollectionModeID tinyint NOT NULL, RfSpoilageCollectionModeID tinyint NOT NULL, RfProductionCollectionModeID tinyint NOT NULL, LastBuildRecordID int, CurrentJobID int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MachineStage/';

DROP TABLE IF EXISTS MEStemp.MachineStage_Custom;

Create External table IF NOT EXISTS MEStemp.MachineStage_Custom (MachineStageID smallint NOT NULL, ModuleDatabase nvarchar(100), IntendedWOID int, MachineStageCode nvarchar(100), CurrentLaminateSize int, Color nvarchar(100), ModifiedDT datetime, CleanState int, CleaningCycleNo int, CleaningLaminatePartID int, CleaningLaminateSize int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MachineStage_Custom/';

DROP TABLE IF EXISTS MEStemp.MachineStageCell;

Create External table IF NOT EXISTS MEStemp.MachineStageCell (MachineStageCellID smallint NOT NULL, MachineStageID smallint NOT NULL, IsCreator tinyint NOT NULL, IsConsumer tinyint NOT NULL, IsLabeller tinyint NOT NULL, IsAutoBatchNo tinyint NOT NULL, IsPackConsumer tinyint NOT NULL, IsResortConsumer tinyint NOT NULL, IsNonJob tinyint NOT NULL, IsToolShowCavities tinyint NOT NULL, IsInvPackPackNo tinyint NOT NULL, IsInvPackPartNo tinyint NOT NULL, IsInvPackPartDesc tinyint NOT NULL, IsInvPackPartType tinyint NOT NULL, IsInvPackQty tinyint NOT NULL, IsInvPackHeader tinyint NOT NULL, IsInvPackRows tinyint NOT NULL, IsInvPackState tinyint NOT NULL, IsInvPackReason tinyint NOT NULL, IsAcqDblck tinyint NOT NULL, AcqGrid nvarchar(508), CustomPage nvarchar(508), IsClickable tinyint NOT NULL, IsDrillable tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MachineStageCell/';

DROP TABLE IF EXISTS MEStemp.MachineStageEvent;

Create External table IF NOT EXISTS MEStemp.MachineStageEvent (MachineStageEventID int NOT NULL, LoginID smallint NOT NULL, DT datetime NOT NULL, MachineStageID smallint NOT NULL, MachineStageEventTypeID smallint NOT NULL, Comments nvarchar(4000),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MachineStageEvent/';

DROP TABLE IF EXISTS MEStemp.MachineStageEventType;

Create External table IF NOT EXISTS MEStemp.MachineStageEventType (MachineStageEventTypeID smallint NOT NULL, MachineStageEventType nvarchar(100) NOT NULL, Description nvarchar(508), MachineStageTypeID smallint NOT NULL, MachineStageEventTypeCode nvarchar(100), IsAlarm tinyint NOT NULL, AlarmText nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MachineStageEventType/';

DROP TABLE IF EXISTS MEStemp.MachineStageInvPosition;

Create External table IF NOT EXISTS MEStemp.MachineStageInvPosition (MachineStageInvPositionId smallint NOT NULL, MachineStageId smallint NOT NULL, Position nvarchar(100) NOT NULL, PositionNumber tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MachineStageInvPosition/';

DROP TABLE IF EXISTS MEStemp.MachineStageType;

Create External table IF NOT EXISTS MEStemp.MachineStageType (MachineStageTypeID smallint NOT NULL, Name nvarchar(508) NOT NULL, Description nvarchar(508), MachineStageTypeCode nvarchar(100),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MachineStageType/';

DROP TABLE IF EXISTS MEStemp.MachineStop;

Create External table IF NOT EXISTS MEStemp.MachineStop (MachineStopID int NOT NULL, MachineStopReasonID smallint NOT NULL, FromDT datetime NOT NULL, ToDT datetime NOT NULL, MachineStageID smallint NOT NULL, IsEdited tinyint NOT NULL, RevisionNo tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MachineStop/';

DROP TABLE IF EXISTS MEStemp.MachineStop_;

Create External table IF NOT EXISTS MEStemp.MachineStop_ (MachineStopID int NOT NULL, StopComments nvarchar(2048), StartComments nvarchar(2048), StartLoginID smallint NOT NULL, StopLoginID smallint NOT NULL, OpenedStopDT datetime NOT NULL, SavedStopDT datetime NOT NULL, OpenedStartDT datetime NOT NULL, SavedStartDT datetime NOT NULL, OriginalLoginID smallint NOT NULL, EditedMachineStopID int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MachineStop_/';

DROP TABLE IF EXISTS MEStemp.MachineStopCategory;

Create External table IF NOT EXISTS MEStemp.MachineStopCategory (MachineStopCategoryID smallint NOT NULL, MachineStopCategory nvarchar(508) NOT NULL, Description nvarchar(508), MachineStageTypeID smallint NOT NULL, StopCategoryCode nvarchar(100), DisplayOrder smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MachineStopCategory/';

DROP TABLE IF EXISTS MEStemp.MachineStopReason;

Create External table IF NOT EXISTS MEStemp.MachineStopReason (MachineStopReasonID smallint NOT NULL, StopReason nvarchar(508) NOT NULL, Description nvarchar(508), MachineStageTypeID smallint NOT NULL, IsPlanned tinyint NOT NULL, StopCode nvarchar(100), RfMachineStopClassID tinyint NOT NULL, RfLineStopReasonTypeID smallint NOT NULL, MachineStopCategoryID smallint, IsRetired tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MachineStopReason/';

DROP TABLE IF EXISTS MEStemp.MachineStopReasonGroup;

Create External table IF NOT EXISTS MEStemp.MachineStopReasonGroup (MachineStopReasonGroupID smallint NOT NULL, MachineStopReasonGroup nvarchar(508) NOT NULL, Description nvarchar(508), MachineStopReasonGroupCode nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MachineStopReasonGroup/';

DROP TABLE IF EXISTS MEStemp.MaintenanceManHourPlanned;

Create External table IF NOT EXISTS MEStemp.MaintenanceManHourPlanned (MaintenanceManHourPlannedID int NOT NULL, MaintenanceProcedureID int NOT NULL, PlannedManHours float NOT NULL, PersonnelGroupID smallint, SupplierID smallint NOT NULL, SupplierCost float NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MaintenanceManHourPlanned/';

DROP TABLE IF EXISTS MEStemp.MaintenanceManHours;

Create External table IF NOT EXISTS MEStemp.MaintenanceManHours (MaintenanceManHoursID int NOT NULL, MaintenanceWorkID int NOT NULL, WorkerLoginID smallint NOT NULL, ActualManHours float NOT NULL, RevisionNo smallint NOT NULL, IsEdited tinyint NOT NULL, ModifiedDT datetime NOT NULL, SupplierID smallint NOT NULL, SupplierCost float NOT NULL, PersonnelGroupID smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MaintenanceManHours/';

DROP TABLE IF EXISTS MEStemp.MaintenancePartPlanned;

Create External table IF NOT EXISTS MEStemp.MaintenancePartPlanned (MaintenancePartPlannedID int NOT NULL, MaintenanceProcedureStepID int NOT NULL, PartID int NOT NULL, PlannedCost float, IsOptional tinyint NOT NULL, Quantity float NOT NULL, UnitID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MaintenancePartPlanned/';

DROP TABLE IF EXISTS MEStemp.MaintenancePartUsed;

Create External table IF NOT EXISTS MEStemp.MaintenancePartUsed (MaintenancePartUsedID int NOT NULL, MaintenanceWorkID int NOT NULL, MaintenanceProcedureResultID int, PartID int NOT NULL, Cost float NOT NULL, DT datetime NOT NULL, LoginID smallint NOT NULL, Quantity float NOT NULL, UnitID smallint NOT NULL, InventoryPackID int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MaintenancePartUsed/';

DROP TABLE IF EXISTS MEStemp.MaintenanceProcedure;

Create External table IF NOT EXISTS MEStemp.MaintenanceProcedure (MaintenanceProcedureID int NOT NULL, MaintenanceProcedure nvarchar(508) NOT NULL, PartID int, ResponsibleDepartmentID smallint NOT NULL, BudgetWorkDone float, PlannedManHours float, PlannedDuration float NOT NULL, MaintenanceProcedureCode nvarchar(50), AdvanceWarningPeriod int NOT NULL, SchedulingWindow int NOT NULL, PlannedMachineStageDowntime int NOT NULL, PlannedLineDowntime int NOT NULL, SubPartID int, CreatedDT datetime NOT NULL, CreatedLoginID smallint NOT NULL, RfMaintenanceWorkTypeID smallint NOT NULL, CostCentreID smallint NOT NULL, IsAutoAuthoriseWork tinyint NOT NULL, IsRetired tinyint NOT NULL, IsToolTakenOutOfService tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MaintenanceProcedure/';

DROP TABLE IF EXISTS MEStemp.MaintenanceProcedureGroup;

Create External table IF NOT EXISTS MEStemp.MaintenanceProcedureGroup (MaintenanceProcedureGroupID int NOT NULL, MaintenanceProcedureGroup nvarchar(100) NOT NULL, Description nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MaintenanceProcedureGroup/';

DROP TABLE IF EXISTS MEStemp.MaintenanceProcedureResult;

Create External table IF NOT EXISTS MEStemp.MaintenanceProcedureResult (MaintenanceProcedureResultID int NOT NULL, MaintenanceProcedureStepID int, MaintenanceWorkID int NOT NULL, Result float, ResultPassFail smallint NOT NULL, Comments nvarchar(508), DT datetime NOT NULL, CheckedByLoginID smallint NOT NULL, MaintenanceProcedureStep nvarchar(2000), StepOrder float, SubPartID int, Component nvarchar(508), RfMaintenanceCheckTypeID smallint NOT NULL, RfSpecialActionTypeID tinyint, Test nvarchar(508), USL float, LSL float, PersonnelGroupID smallint, SupplierID smallint NOT NULL, Nom float, Units nvarchar(40), DecPlaces tinyint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MaintenanceProcedureResult/';

DROP TABLE IF EXISTS MEStemp.MaintenanceProcedureRevision;

Create External table IF NOT EXISTS MEStemp.MaintenanceProcedureRevision (MaintenanceProcedureRevisionID int NOT NULL, MaintenanceProcedureID int NOT NULL, FromDT datetime NOT NULL, ToDT datetime NOT NULL, IsEdited tinyint NOT NULL, RevisionNo smallint NOT NULL, LoginID smallint NOT NULL, ModifiedDT datetime NOT NULL, RevisedMaintProcID int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MaintenanceProcedureRevision/';

DROP TABLE IF EXISTS MEStemp.MaintenanceProcedureStep;

Create External table IF NOT EXISTS MEStemp.MaintenanceProcedureStep (MaintenanceProcedureStepID int NOT NULL, MaintenanceProcedureStep nvarchar(2000) NOT NULL, MaintenanceProcedureRevisionID int, StepOrder float NOT NULL, SubPartID int, Component nvarchar(508), RfMaintenanceCheckTypeID smallint NOT NULL, RfSpecialActionTypeID tinyint NOT NULL, Test nvarchar(508) NOT NULL, USL float, LSL float, PersonnelGroupID smallint, SupplierID smallint NOT NULL, Nom float, Units nvarchar(40), DecPlaces tinyint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MaintenanceProcedureStep/';

DROP TABLE IF EXISTS MEStemp.MaintenanceReason;

Create External table IF NOT EXISTS MEStemp.MaintenanceReason (MaintenanceReasonID smallint NOT NULL, MaintenanceReason nvarchar(508) NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MaintenanceReason/';

DROP TABLE IF EXISTS MEStemp.MaintenanceWork;

Create External table IF NOT EXISTS MEStemp.MaintenanceWork (MaintenanceWorkID int NOT NULL, MaintenanceWO nvarchar(508), AssetID int NOT NULL, MaintenanceProcedureID int, MaintenanceProcedureRevisionID int, MaintProcScheduleOnAssetID int, RfMaintenanceWorkTypeID smallint NOT NULL, AdhocWorkToDo nvarchar(508), MaintProcScheduleOnAssetNo int, MaintenanceWorkStatusID smallint NOT NULL, ActualStartDT datetime NOT NULL, ActualEndDT datetime NOT NULL, ActualManHours float NOT NULL, PlannedStartDT datetime, PlannedEndDT datetime, PlannedManHours float, OwnerLoginID smallint NOT NULL, ActualLabourCost float, PlannedLabourCost float, RfCurrencyID smallint NOT NULL, PartsCost float, LineDownCost float, Priority smallint, MaintenanceReasonID smallint NOT NULL, IsPlannedDateLockedByUser tinyint NOT NULL, EBATriggerID int, PeriodDueDT datetime, RunHoursDueDT datetime, PartsBasedDueDT datetime, EventDueDT datetime, ReleasedDT datetime, ReleasedLoginID smallint, IsEdited tinyint NOT NULL, RevisionNo smallint NOT NULL, AbsoluteCounterThreshold int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MaintenanceWork/';

DROP TABLE IF EXISTS MEStemp.MaintenanceWork_;

Create External table IF NOT EXISTS MEStemp.MaintenanceWork_ (MaintenanceWorkID int NOT NULL, WorkDone nvarchar(2000), Comments nvarchar(508), LoginID smallint NOT NULL, ModifiedDT datetime NOT NULL, Reference1 nvarchar(508), Reference2 nvarchar(508), Reference3 nvarchar(508), Reference4 nvarchar(508), Reference5 nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MaintenanceWork_/';

DROP TABLE IF EXISTS MEStemp.MaintenanceWorkStatus;

Create External table IF NOT EXISTS MEStemp.MaintenanceWorkStatus (MaintenanceWorkStatusID smallint NOT NULL, MaintenanceWorkStatus nvarchar(508) NOT NULL, Description nvarchar(508), RfMaintenanceWorkStatusID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MaintenanceWorkStatus/';

DROP TABLE IF EXISTS MEStemp.MaintProcScheduleOnAsset;

Create External table IF NOT EXISTS MEStemp.MaintProcScheduleOnAsset (MaintProcScheduleOnAssetID int NOT NULL, MaintenanceProcedureID int NOT NULL, AssetID int NOT NULL, RfPeriodTypeID smallint NOT NULL, StartDate datetime, AssetCountTypeID smallint, EBAReference nvarchar(100), IsEdited tinyint NOT NULL, LoginID smallint NOT NULL, ModifiedDT datetime, CalendarN1 int, CalendarN2 int, CalendarN3 int, CounterThreshold1 int, CounterThreshold2 int, CounterThreshold3 int, EstimatedCountPerDay float, RunHourThreshold1 int, RunHourThreshold2 int, RunHourThreshold3 int, EstimatedRunHoursPerDay float, EstimatedEventsPerDay float, RevisionNo smallint NOT NULL, IsRetired tinyint NOT NULL, PriorExecutions smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MaintProcScheduleOnAsset/';

DROP TABLE IF EXISTS MEStemp.MeasurementSet;

Create External table IF NOT EXISTS MEStemp.MeasurementSet (MeasurementSetID int NOT NULL, ActualAttrSampleSize int, AcquisitionContextID int NOT NULL, IsEdited tinyint NOT NULL, DT datetime NOT NULL, AcquisitionID smallint NOT NULL, MachineStageID smallint NOT NULL, RfAcquisitionClassID tinyint NOT NULL, RevisionNo tinyint NOT NULL, PartID int NOT NULL, JobID int NOT NULL, ResumeStep smallint NOT NULL, InventoryPackID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MeasurementSet/';

DROP TABLE IF EXISTS MEStemp.MeasurementSet_;

Create External table IF NOT EXISTS MEStemp.MeasurementSet_ (MeasurementSetID int NOT NULL, Comments nvarchar(4000), LoginID smallint NOT NULL, OpenedDT datetime NOT NULL, SavedDT datetime NOT NULL, OriginalLoginID smallint NOT NULL, EditedMSID int, ApprovalLoginID smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MeasurementSet_/';

DROP TABLE IF EXISTS MEStemp.MeasurementSetP;

Create External table IF NOT EXISTS MEStemp.MeasurementSetP (MeasurementSetPID int NOT NULL, ActualAttrSampleSize int, AcquisitionContextID int NOT NULL, IsEdited tinyint NOT NULL, DT datetime NOT NULL, AcquisitionID smallint NOT NULL, MachineStageID smallint NOT NULL, RfAcquisitionClassID smallint NOT NULL, RevisionNo tinyint NOT NULL, PartID int NOT NULL, JobID int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MeasurementSetP/';

DROP TABLE IF EXISTS MEStemp.MeasurementSetRequired;

Create External table IF NOT EXISTS MEStemp.MeasurementSetRequired (MeasurementSetRequiredID int NOT NULL, EBATriggerID int NOT NULL, AcquisitionContextID int NOT NULL, MeasurementSetID int NOT NULL, RfAcquisitionStatusID smallint NOT NULL, SubmittedDT datetime NOT NULL, DueDT datetime NOT NULL, DeadlineDT datetime NOT NULL, ExpiryDT datetime NOT NULL, OwnerID smallint NOT NULL, ActionID int, JobID int, PartID int, AssignedLoginID int, InventoryPackID int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/MeasurementSetRequired/';

DROP TABLE IF EXISTS MEStemp.ModulePackedDateTime;

Create External table IF NOT EXISTS MEStemp.ModulePackedDateTime (SerialNo nvarchar(508), TimePacked datetime, IMS_No nvarchar(100),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ModulePackedDateTime/';

DROP TABLE IF EXISTS MEStemp.ModulePackedDateTime_Buffer;

Create External table IF NOT EXISTS MEStemp.ModulePackedDateTime_Buffer (SerialNo nvarchar(508) NOT NULL, TimePacked datetime, IMS_No nvarchar(100),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ModulePackedDateTime_Buffer/';

DROP TABLE IF EXISTS MEStemp.OPCClient;

Create External table IF NOT EXISTS MEStemp.OPCClient (OPCClientID smallint NOT NULL, OPCClient nvarchar(508) NOT NULL, ComputerID smallint NOT NULL, ModifiedDT datetime NOT NULL, LastUpdateDT datetime NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/OPCClient/';

DROP TABLE IF EXISTS MEStemp.OPCConstant;

Create External table IF NOT EXISTS MEStemp.OPCConstant (OPCConstantID int NOT NULL, OPCItemID int NOT NULL, Description nvarchar(100), Constant float NOT NULL, Formula nvarchar(2000), ModifiedDT datetime NOT NULL, LoginID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/OPCConstant/';

DROP TABLE IF EXISTS MEStemp.OPCCounter;

Create External table IF NOT EXISTS MEStemp.OPCCounter (OPCCounterID int NOT NULL, OPCItemID int NOT NULL, Description nvarchar(508), LoValue bigint, HiValue bigint, Scale float NOT NULL, Offset float NOT NULL, Formula nvarchar(2000), UnitCountTypeID smallint, ScrapReasonID smallint, ModifiedDT datetime NOT NULL, LoginID smallint NOT NULL, IsProduction tinyint, AcceptableRateOfChange float, RfDeltaValueModeID tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/OPCCounter/';

DROP TABLE IF EXISTS MEStemp.OPCDigital;

Create External table IF NOT EXISTS MEStemp.OPCDigital (OPCDigitalID int NOT NULL, OPCItemID int NOT NULL, Description nvarchar(508), Formula nvarchar(2000), VariableTypeID int, HeadNo smallint NOT NULL, SampleNo smallint NOT NULL, PositionNo smallint NOT NULL, ModifiedDT datetime NOT NULL, LoginID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/OPCDigital/';

DROP TABLE IF EXISTS MEStemp.OPCGroup;

Create External table IF NOT EXISTS MEStemp.OPCGroup (OPCGroupID int NOT NULL, Deadband float, UpdateRate int, OPCGroup nvarchar(100) NOT NULL, IsActive tinyint, RfGroupNotifyModeID tinyint NOT NULL, TimeBias int, OPCServerID smallint, IsPublic tinyint, LocalID smallint, CacheUpdateRate int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/OPCGroup/';

DROP TABLE IF EXISTS MEStemp.OPCItem;

Create External table IF NOT EXISTS MEStemp.OPCItem (OPCItemID int NOT NULL, OPCServerID smallint NOT NULL, OPCGroupID int, TagName nvarchar(100) NOT NULL, TagAddress nvarchar(508) NOT NULL, RfOPCItemTypeID tinyint NOT NULL, IsNotify tinyint NOT NULL, RawValue nvarchar(40), UpdatedDT datetime NOT NULL, Quality nvarchar(100), Value nvarchar(40), LastRawValue nvarchar(40), LastUpdatedDT datetime NOT NULL, LastQuality nvarchar(100), LastValue nvarchar(40), MachineStageID smallint, ChangeAction nvarchar(508), PLCID smallint NOT NULL, IsDatedOnServer tinyint NOT NULL, IsStoredAsArray tinyint NOT NULL, IsActive tinyint NOT NULL, MachineStageEventTypeID smallint, RfTransitionTypeID tinyint NOT NULL, TransitionToValue nvarchar(100),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/OPCItem/';

DROP TABLE IF EXISTS MEStemp.OPCItemHistory;

Create External table IF NOT EXISTS MEStemp.OPCItemHistory (OPCItemHistoryID int NOT NULL, OPCItemID int NOT NULL, UpdatedDT datetime NOT NULL, RawValue nvarchar(40), Value nvarchar(40), Quality nvarchar(100), CommittedDT datetime NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/OPCItemHistory/';

DROP TABLE IF EXISTS MEStemp.OPCMachineEvent;

Create External table IF NOT EXISTS MEStemp.OPCMachineEvent (OPCMachineEventID int NOT NULL, OPCItemID int NOT NULL, Description nvarchar(508), Formula nvarchar(2000), ModifiedDT datetime NOT NULL, LoginID smallint NOT NULL, RfMachineStopRecordModeID tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/OPCMachineEvent/';

DROP TABLE IF EXISTS MEStemp.OPCMachineEventCode;

Create External table IF NOT EXISTS MEStemp.OPCMachineEventCode (OPCMachineEventCodeID int NOT NULL, OPCMachineEventCode nvarchar(100) NOT NULL, OPCMachineEventID int NOT NULL, MachineStopReasonID smallint, IsRunning tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/OPCMachineEventCode/';

DROP TABLE IF EXISTS MEStemp.OPCParameter;

Create External table IF NOT EXISTS MEStemp.OPCParameter (OPCParameterID int NOT NULL, OPCItemID int NOT NULL, Description nvarchar(508), Scale float NOT NULL, Offset float NOT NULL, Formula nvarchar(2000), VariableTypeID int, HeadNo smallint NOT NULL, SampleNo smallint NOT NULL, PositionNo smallint NOT NULL, ModifiedDT datetime NOT NULL, LoginID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/OPCParameter/';

DROP TABLE IF EXISTS MEStemp.OPCServer;

Create External table IF NOT EXISTS MEStemp.OPCServer (OPCServerID smallint NOT NULL, OPCClientID smallint NOT NULL, OPCServer nvarchar(508) NOT NULL, ProgID nvarchar(508) NOT NULL, IsActive tinyint NOT NULL, ComputerID smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/OPCServer/';

DROP TABLE IF EXISTS MEStemp.OPCSpeed;

Create External table IF NOT EXISTS MEStemp.OPCSpeed (OPCSpeedID int NOT NULL, OPCItemID int NOT NULL, Description nvarchar(508), Scale float NOT NULL, Formula nvarchar(2000), ModifiedDT datetime NOT NULL, LoginID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/OPCSpeed/';

DROP TABLE IF EXISTS MEStemp.OPCText;

Create External table IF NOT EXISTS MEStemp.OPCText (OPCTextID int NOT NULL, OPCItemID int NOT NULL, Description nvarchar(508), ModifiedDT datetime NOT NULL, LoginID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/OPCText/';

DROP TABLE IF EXISTS MEStemp.Operation;

Create External table IF NOT EXISTS MEStemp.Operation (OperationID int NOT NULL, OperationTypeID smallint NOT NULL, MachineStageID smallint NOT NULL, JobID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Operation/';

DROP TABLE IF EXISTS MEStemp.OperationType;

Create External table IF NOT EXISTS MEStemp.OperationType (OperationTypeID smallint NOT NULL, OperationType nvarchar(508) NOT NULL, OperationTypeCode nvarchar(100), Description nvarchar(508), PartTypeID smallint NOT NULL, Sequence smallint NOT NULL, IsOptional tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/OperationType/';

DROP TABLE IF EXISTS MEStemp.Part;

Create External table IF NOT EXISTS MEStemp.Part (PartID int NOT NULL, PartNumber nvarchar(508) NOT NULL, Description nvarchar(508), IsRetired tinyint NOT NULL, BaseUnitID smallint NOT NULL, PartTypeID smallint NOT NULL, Reference1 nvarchar(508), Reference2 nvarchar(508), Reference3 nvarchar(508), Reference4 nvarchar(508), Reference5 nvarchar(508), Variant1 nvarchar(200), Variant2 nvarchar(200), Variant3 nvarchar(200), Variant4 nvarchar(200), Variant5 nvarchar(200), Variant6 nvarchar(200), Variant7 nvarchar(200), Variant8 nvarchar(200), Variant9 nvarchar(200), Variant10 nvarchar(200), Variant11 nvarchar(200), Variant12 nvarchar(200), Variant13 nvarchar(200), Variant14 nvarchar(200), Variant15 nvarchar(200), Variant16 nvarchar(200), Variant17 nvarchar(200), Variant18 nvarchar(200), Variant19 nvarchar(200), Variant20 nvarchar(200),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Part/';

DROP TABLE IF EXISTS MEStemp.Part_;

Create External table IF NOT EXISTS MEStemp.Part_ (PartID int NOT NULL, TheoreticalSpeed float, LabelsPerInventoryPack smallint, ItemsPerInventoryPack float, ItemsPerLayer float, LayersPerInventoryPack int, PackingCode nvarchar(100), LabelTemplate nvarchar(508), ProductionInstructions nvarchar(2000), QualityInstructions nvarchar(2000), CustomerPartNo nvarchar(508), CustomerPartDescription nvarchar(508), LabelText1 nvarchar(508), LabelText2 nvarchar(508), LabelText3 nvarchar(508), UnitPrice1 float, UnitPrice2 float, LegacyPartNo nvarchar(508), CreateDT datetime NOT NULL, ModifiedDT datetime NOT NULL, LoginID smallint NOT NULL, PartRevisionNo nvarchar(100), PartClassificationID smallint NOT NULL, LastPurchaseCost float, AveragePurchaseCost float, StandardPurchaseCost float, StandardManufactureCost float, StandardSaleCost float, PartSubClassificationID smallint NOT NULL, CreatedInvUseByPeriod int, CreatedInvUseAfterPeriod int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Part_/';

DROP TABLE IF EXISTS MEStemp.Part_custom;

Create External table IF NOT EXISTS MEStemp.Part_custom (PartID int NOT NULL, Txn_date datetime, Inventory_Item_Id int, Item_Type_Description nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Part_custom/';

DROP TABLE IF EXISTS MEStemp.PartClassification;

Create External table IF NOT EXISTS MEStemp.PartClassification (PartClassificationID smallint NOT NULL, PartClassification nvarchar(508) NOT NULL, RfPartClassificationID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/PartClassification/';

DROP TABLE IF EXISTS MEStemp.PartInstance;

Create External table IF NOT EXISTS MEStemp.PartInstance (PartInstanceID int NOT NULL, MeasurementSetID int NOT NULL, HeadNo smallint NOT NULL, SampleNo smallint NOT NULL, SerialNo nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/PartInstance/';

DROP TABLE IF EXISTS MEStemp.PartSubClassification;

Create External table IF NOT EXISTS MEStemp.PartSubClassification (PartSubClassificationID smallint NOT NULL, PartSubClassification nvarchar(508) NOT NULL, PartClassificationID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/PartSubClassification/';

DROP TABLE IF EXISTS MEStemp.PartType;

Create External table IF NOT EXISTS MEStemp.PartType (PartTypeID smallint NOT NULL, PartType nvarchar(508) NOT NULL, Description nvarchar(508), PartTypeCode nvarchar(100), Variant1TagListID smallint, Variant2TagListID smallint, Variant3TagListID smallint, Variant4TagListID smallint, Variant5TagListID smallint, Variant6TagListID smallint, Variant7TagListID smallint, Variant8TagListID smallint, Variant9TagListID smallint, Variant10TagListID smallint, Variant11TagListID smallint, Variant12TagListID smallint, Variant13TagListID smallint, Variant14TagListID smallint, Variant15TagListID smallint, Variant16TagListID smallint, Variant17TagListID smallint, Variant18TagListID smallint, Variant19TagListID smallint, Variant20TagListID smallint, RfInventoryAccountingModeID tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/PartType/';

DROP TABLE IF EXISTS MEStemp.PLC;

Create External table IF NOT EXISTS MEStemp.PLC (PLCID smallint NOT NULL, PLC varchar(50) NOT NULL, Description nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/PLC/';

DROP TABLE IF EXISTS MEStemp.Product;

Create External table IF NOT EXISTS MEStemp.Product (ProductID int NOT NULL, ProductTypeID smallint NOT NULL, Name nvarchar(508) NOT NULL, Description nvarchar(508), IsRetired tinyint NOT NULL, PartID int, Variant1 nvarchar(50), Variant2 nvarchar(50), Variant3 nvarchar(50), Variant4 nvarchar(50), Variant5 nvarchar(50), Variant6 nvarchar(50), Variant7 nvarchar(50), Variant8 nvarchar(50), Variant9 nvarchar(50), Variant10 nvarchar(50), Variant11 nvarchar(50), Variant12 nvarchar(50), Variant13 nvarchar(50), Variant14 nvarchar(50), Variant15 nvarchar(50), Variant16 nvarchar(50), Variant17 nvarchar(50), Variant18 nvarchar(50), Variant19 nvarchar(50), Variant20 nvarchar(50),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Product/';

DROP TABLE IF EXISTS MEStemp.ProductionStop;

Create External table IF NOT EXISTS MEStemp.ProductionStop (ProductionStopID int NOT NULL, ProductionStopReasonID smallint NOT NULL, JobID int NOT NULL, FromDT datetime NOT NULL, ToDT datetime NOT NULL, LineID smallint NOT NULL, IsEdited tinyint NOT NULL, RevisionNo tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ProductionStop/';

DROP TABLE IF EXISTS MEStemp.ProductionStop_;

Create External table IF NOT EXISTS MEStemp.ProductionStop_ (ProductionStopID int NOT NULL, StopComments nvarchar(508), StopLoginID smallint NOT NULL, OpenedStopDT datetime NOT NULL, SavedStopDT datetime NOT NULL, OriginalLoginID smallint NOT NULL, EditedProductionStopID int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ProductionStop_/';

DROP TABLE IF EXISTS MEStemp.ProductionStopReason;

Create External table IF NOT EXISTS MEStemp.ProductionStopReason (ProductionStopReasonID smallint NOT NULL, ProductionStopReason nvarchar(508) NOT NULL, Description nvarchar(508), StopCode nvarchar(100), RfLineStopReasonTypeID smallint NOT NULL, RfStopRelativeToJobID tinyint NOT NULL, IsPlanned tinyint NOT NULL, IsDefaultNoJobReason tinyint NOT NULL, IsRetired tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ProductionStopReason/';

DROP TABLE IF EXISTS MEStemp.ProductType;

Create External table IF NOT EXISTS MEStemp.ProductType (ProductTypeID smallint NOT NULL, Name nvarchar(508) NOT NULL, ProductTypeCode nvarchar(100), Variant1TagListID smallint, Variant2TagListID smallint, Variant3TagListID smallint, Variant4TagListID smallint, Variant5TagListID smallint, Variant6TagListID smallint, Variant7TagListID smallint, Variant8TagListID smallint, Variant9TagListID smallint, Variant10TagListID smallint, Variant11TagListID smallint, Variant12TagListID smallint, Variant13TagListID smallint, Variant14TagListID smallint, Variant15TagListID smallint, Variant16TagListID smallint, Variant17TagListID smallint, Variant18TagListID smallint, Variant19TagListID smallint, Variant20TagListID smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ProductType/';

DROP TABLE IF EXISTS MEStemp.Query;

Create External table IF NOT EXISTS MEStemp.Query (MeasurementSetID int NOT NULL, SerialNo nvarchar(508) NOT NULL, BinNo nvarchar(508) NOT NULL, Komax1 nvarchar(508), CktFormBuildMachine nvarchar(508), ElBuildMachine nvarchar(508), Comments nvarchar(4000), DT datetime2(3) NOT NULL, NumCells int, CellHole int, LiftedTab int, MultipleCrack int, DiagonalCrack int, ReversePolarity int, ShatteredCell int, ShortedPasteBusbar int, ShortedPasteMiddle int, ShortedPasteUnknown int, VerticalCrack int, VisibleCrack int, Weird int, CellReplacementFab int, CellReplacementMod int, TouchUpthenGood int, WipethenGood int, Rework int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Query/';

DROP TABLE IF EXISTS MEStemp.RootCause;

Create External table IF NOT EXISTS MEStemp.RootCause (RootCauseID int NOT NULL, RootCause nvarchar(508) NOT NULL, Comments nvarchar(508), RootCauseTypeID smallint NOT NULL, LineTypeID smallint, MachineStageTypeID smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/RootCause/';

DROP TABLE IF EXISTS MEStemp.RootCauseType;

Create External table IF NOT EXISTS MEStemp.RootCauseType (RootCauseTypeID smallint NOT NULL, RootCauseType nvarchar(508) NOT NULL, Description nvarchar(2000), RootCauseTypeCode nvarchar(100),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/RootCauseType/';

DROP TABLE IF EXISTS MEStemp.Scrap;

Create External table IF NOT EXISTS MEStemp.Scrap (ScrapID int NOT NULL, ScrapReasonID smallint NOT NULL, PartID int NOT NULL, Quantity float NOT NULL, UnitID smallint NOT NULL, DT datetime NOT NULL, MachineStageID smallint NOT NULL, IsEdited tinyint NOT NULL, RevisionNo tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Scrap/';

DROP TABLE IF EXISTS MEStemp.Scrap_;

Create External table IF NOT EXISTS MEStemp.Scrap_ (ScrapID int NOT NULL, CollectionQuantity float NOT NULL, CollectionUnitID smallint NOT NULL, Comments nvarchar(508), LoginID smallint NOT NULL, OpenedDT datetime NOT NULL, SavedDT datetime NOT NULL, OriginalLoginID smallint NOT NULL, EditedScrapID int, Reference1 nvarchar(508), Reference2 nvarchar(508), Reference3 nvarchar(508), Reference4 nvarchar(508), Reference5 nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Scrap_/';

DROP TABLE IF EXISTS MEStemp.ScrapReason;

Create External table IF NOT EXISTS MEStemp.ScrapReason (ScrapReasonID smallint NOT NULL, ScrapReason nvarchar(100) NOT NULL, Description nvarchar(508), MachineStageTypeID smallint NOT NULL, DefaultUnitID smallint NOT NULL, IsRetired tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ScrapReason/';

DROP TABLE IF EXISTS MEStemp.ScrapReasonGroup;

Create External table IF NOT EXISTS MEStemp.ScrapReasonGroup (ScrapReasonGroupID smallint NOT NULL, ScrapReasonGroup nvarchar(508) NOT NULL, Description nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ScrapReasonGroup/';

DROP TABLE IF EXISTS MEStemp.Sequences;

Create External table IF NOT EXISTS MEStemp.Sequences (SeqName varchar(50) NOT NULL, SeqValue smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Sequences/';

DROP TABLE IF EXISTS MEStemp.SettingsPlantDefaults;

Create External table IF NOT EXISTS MEStemp.SettingsPlantDefaults (SettingsPlantDefaultsID smallint NOT NULL, StartOfDay varchar(10) NOT NULL, StartOfWeek smallint NOT NULL, StartOfYear smallint NOT NULL, RfLocaleID smallint NOT NULL, RfDefaultCurrencyID tinyint NOT NULL, DataEntryDefaultURI nvarchar(508), OnDataCommitGoTo varchar(50) NOT NULL, ChainToStopped smallint NOT NULL, ColourStagesForCurrentJob smallint NOT NULL, UnAckAlarmsPeriod smallint NOT NULL, ShowOverdueIcon smallint NOT NULL, ShowAlarmIcon smallint NOT NULL, WONumberModeID tinyint NOT NULL, IsShowVarClassCombo tinyint NOT NULL, IsShowAcqClassCombo tinyint NOT NULL, RfMachineStopRecordModeID tinyint NOT NULL, IsAutoCreateProductsForParts tinyint NOT NULL, IsAutoCreateProductTypeForPart tinyint NOT NULL, EnforceClosedAlarms tinyint NOT NULL, ColourOrder varchar(50) NOT NULL, IsAutoReleaseJob tinyint NOT NULL, JobClose tinyint NOT NULL, IsAllowDuplicateWONumbers tinyint NOT NULL, WoClose tinyint NOT NULL, IsOverdueRefToJob tinyint NOT NULL, DefaultSPCCalcStyleID smallint NOT NULL, IsAllowInventoryInserts tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/SettingsPlantDefaults/';

DROP TABLE IF EXISTS MEStemp.SettingsPlantNameTemplates;

Create External table IF NOT EXISTS MEStemp.SettingsPlantNameTemplates (SettingsPlantNameTemplatesID int NOT NULL, CreateInvPackNoTemplate nvarchar(508) NOT NULL, CustomerUniqueKeyTemplate nvarchar(508), InventoryPackNameTemplate nvarchar(508), WONumberTemplate nvarchar(508), ReceiveInvPackNoTemplate nvarchar(508) NOT NULL, BOMVariantTemplate nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/SettingsPlantNameTemplates/';

DROP TABLE IF EXISTS MEStemp.SettingsPlantTimeWindows;

Create External table IF NOT EXISTS MEStemp.SettingsPlantTimeWindows (SettingsPlantTimeWindowsID smallint NOT NULL, JobHistory int NOT NULL, ProductionSpoilageHistory int NOT NULL, DowntimeHistory int NOT NULL, InventoryHistory int NOT NULL, InventoryLabelHistory int NOT NULL, BuildRecordHistory int NOT NULL, LineInfoHistory int NOT NULL, QualityMeasurementHistory int NOT NULL, ProcessParameterHistory int NOT NULL, OPCItemHistoryHistory int NOT NULL, InitialLineInfoHistory int NOT NULL, MaintenanceProjectionPeriod int NOT NULL, ShiftLookAheadDays int NOT NULL, ForwardSchedulingWindow int NOT NULL, DefaultMaxTrendRecallWindow int NOT NULL, DefaultConcernDuePeriod int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/SettingsPlantTimeWindows/';

DROP TABLE IF EXISTS MEStemp.Shift;

Create External table IF NOT EXISTS MEStemp.Shift (ShiftID int NOT NULL, ShiftTypeID smallint NOT NULL, ShiftPatternID smallint NOT NULL, FromDT datetime NOT NULL, ToDT datetime NOT NULL, Sequence smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Shift/';

DROP TABLE IF EXISTS MEStemp.ShiftlyChecksDone;

Create External table IF NOT EXISTS MEStemp.ShiftlyChecksDone (ShiftlyChecksDoneID int NOT NULL, shiftid int NOT NULL, jobid int NOT NULL, fromdt datetime NOT NULL, todt datetime NOT NULL, partid int NOT NULL, acquisitioncontextid int NOT NULL, machinestageid int NOT NULL, acquisitionvariantid int, acquisitionid int, downtime float, uptime float, total_time float, datacollectionperiod float, checks_req float, checks_req_after_downtime float, checks_done int, updated_dt datetime,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ShiftlyChecksDone/';

DROP TABLE IF EXISTS MEStemp.ShiftPattern;

Create External table IF NOT EXISTS MEStemp.ShiftPattern (ShiftPatternID smallint NOT NULL, ShiftPattern nvarchar(100) NOT NULL, PatternDuration int NOT NULL, IsPatternDirty tinyint NOT NULL, IsCustomPeriod tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ShiftPattern/';

DROP TABLE IF EXISTS MEStemp.ShiftSequence;

Create External table IF NOT EXISTS MEStemp.ShiftSequence (ShiftSequenceID smallint NOT NULL, ShiftPatternID smallint NOT NULL, ShiftTypeID smallint NOT NULL, Sequence smallint NOT NULL, ShiftStartTime datetime NOT NULL, ShiftEndTime datetime NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ShiftSequence/';

DROP TABLE IF EXISTS MEStemp.ShiftType;

Create External table IF NOT EXISTS MEStemp.ShiftType (ShiftTypeID smallint NOT NULL, Name nvarchar(100) NOT NULL, Description nvarchar(508), IsNonWorking tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ShiftType/';

DROP TABLE IF EXISTS MEStemp.SPCCalcStyle_CUS;

Create External table IF NOT EXISTS MEStemp.SPCCalcStyle_CUS (SPCCalcStyleID smallint NOT NULL, SPCCalcStyle nvarchar(100) NOT NULL, CmThreshold float NOT NULL, CpThreshold float NOT NULL, PpThreshold float NOT NULL, CgThreshold float NOT NULL, RnRThreshold float NOT NULL, IsShowCpOnlyIfStable tinyint NOT NULL, CAMTarget float NOT NULL, CpBasedOnID tinyint NOT NULL, CLBasedOnID tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/SPCCalcStyle_CUS/';

DROP TABLE IF EXISTS MEStemp.SPCChartComponent;

Create External table IF NOT EXISTS MEStemp.SPCChartComponent (SPCChartComponentID int NOT NULL, SPCChartDefID int NOT NULL, SPCVarChartID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/SPCChartComponent/';

DROP TABLE IF EXISTS MEStemp.SPCChartControlLimit;

Create External table IF NOT EXISTS MEStemp.SPCChartControlLimit (SPCChartControlLimitID int NOT NULL, SPCChartComponentID int NOT NULL, RfChartComponentID tinyint NOT NULL, RfChart nvarchar(40) NOT NULL, UCL float, CL float, LCL float, FromDT datetime NOT NULL, ToDT datetime NOT NULL, RangeToDT datetime NOT NULL, ModifiedDT datetime NOT NULL, LoginID smallint NOT NULL, RangeFromDT datetime NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/SPCChartControlLimit/';

DROP TABLE IF EXISTS MEStemp.SPCChartDef;

Create External table IF NOT EXISTS MEStemp.SPCChartDef (SPCChartDefID int NOT NULL, VariableTypeID int NOT NULL, SPCRuleID smallint NOT NULL, RfStudyTypeID smallint NOT NULL, GroupingSize smallint NOT NULL, MovingRange smallint NOT NULL, SPCCalcStyleID smallint NOT NULL, RfGroupingID tinyint NOT NULL, NGroupMarkerID int, CpBasedOnID tinyint NOT NULL, CLBasedOnID tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/SPCChartDef/';

DROP TABLE IF EXISTS MEStemp.SPCChartSet;

Create External table IF NOT EXISTS MEStemp.SPCChartSet (SPCChartSetID smallint NOT NULL, SPCChartSet nvarchar(508) NOT NULL, AcquisitionID smallint, SPCURI nvarchar(508), RfDataBandID tinyint NOT NULL, NPeriods int NOT NULL, SpecTypeID smallint NOT NULL, IsRunningProductOnly tinyint NOT NULL, LHSessionID int, MaxTrendRecallWindow smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/SPCChartSet/';

DROP TABLE IF EXISTS MEStemp.SPCChartSetView;

Create External table IF NOT EXISTS MEStemp.SPCChartSetView (SPCChartSetViewID int NOT NULL, SPCChartSetView nvarchar(508) NOT NULL, Sequence smallint NOT NULL, SPCChartSetID smallint NOT NULL, RfChartSetViewTypeID smallint NOT NULL, NoCols smallint, NoRows smallint, Caption nvarchar(100), Tooltip nvarchar(508), IsAggregated tinyint NOT NULL, RfCutByIDs varchar(50), IsPanelled tinyint NOT NULL, ImageURI nvarchar(508), GridImageRender varchar(50), ParetoXAxis nvarchar(508), ParetoStackBy nvarchar(508), SPCChartSetViewStyleID smallint NOT NULL, IsAutoFit tinyint NOT NULL, OverlayFlags int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/SPCChartSetView/';

DROP TABLE IF EXISTS MEStemp.SPCChartSetViewStyle_CUS;

Create External table IF NOT EXISTS MEStemp.SPCChartSetViewStyle_CUS (SPCChartSetViewStyleID smallint NOT NULL, SPCChartSetViewStyle nvarchar(508) NOT NULL, IsSpecLimits tinyint NOT NULL, IsControlLimitZones tinyint NOT NULL, IsGrid tinyint NOT NULL, IsTimescaled tinyint NOT NULL, IsHAxis tinyint NOT NULL, IsVAxis tinyint NOT NULL, IsXChart tinyint NOT NULL, IsRsChart tinyint NOT NULL, IsRsWChart tinyint NOT NULL, IsDistribution tinyint NOT NULL, IsShowComments tinyint NOT NULL, IsXScatter tinyint NOT NULL, IsIncludeIgnored tinyint NOT NULL, IsOnlyOSOCCharts tinyint NOT NULL, SPCStatsStyleID smallint, IsVerticalOverview tinyint NOT NULL, OverviewWidth smallint NOT NULL, OverviewHeight smallint NOT NULL, PageBreakRfCutByIDs varchar(50), ChartWidth smallint NOT NULL, ChartHeight smallint NOT NULL, IsFixedScale tinyint NOT NULL, IsParetoCurve tinyint NOT NULL, IsLegend tinyint NOT NULL, IsHorizontalDistribution tinyint NOT NULL, IsSigmaLines tinyint NOT NULL, IsShowCAComments tinyint NOT NULL, IsShowUnAckAlarms tinyint NOT NULL, HorizontalOffset smallint NOT NULL, IsShowLimitLabels tinyint NOT NULL, IsColourZones tinyint NOT NULL, IsSystem tinyint NOT NULL, IsOSHotCold tinyint NOT NULL, FixedScaleOverscaling float NOT NULL, IsConstantTitleRow tinyint NOT NULL, IsShowStatCategories tinyint NOT NULL, IsOverlayEnvelope tinyint NOT NULL, IsGradientBackground tinyint NOT NULL, RunChartFont tinyint NOT NULL, DistributionFont tinyint NOT NULL, StatisticsFont tinyint NOT NULL, VAxisFont tinyint NOT NULL, HAxisFont tinyint NOT NULL, OverlayKeyFont tinyint NOT NULL, IsShowOverlayKey tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/SPCChartSetViewStyle_CUS/';

DROP TABLE IF EXISTS MEStemp.SPCDefaultControlLimit;

Create External table IF NOT EXISTS MEStemp.SPCDefaultControlLimit (SPCDefaultControlLimitID int NOT NULL, SPCChartDefID int NOT NULL, RfChartComponentID tinyint NOT NULL, RfChart nvarchar(40) NOT NULL, UCL float, CL float, LCL float, ModifiedDT datetime NOT NULL, LoginID smallint NOT NULL, ProductID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/SPCDefaultControlLimit/';

DROP TABLE IF EXISTS MEStemp.SPCDefaultView;

Create External table IF NOT EXISTS MEStemp.SPCDefaultView (SPCDefaultViewID smallint NOT NULL, Sequence smallint NOT NULL, RfChartSetViewTypeID smallint NOT NULL, NoCols smallint NOT NULL, NoRows smallint NOT NULL, Caption nvarchar(100) NOT NULL, Tooltip nvarchar(508), IsAggregated tinyint NOT NULL, RfCutByIDs varchar(50), IsPanelled tinyint NOT NULL, ImageURI nvarchar(508), GridImageRender varchar(50), ParetoXAxis nvarchar(508), ParetoStackBy nvarchar(508), SPCChartSetViewStyleID smallint NOT NULL, IsAutoFit tinyint NOT NULL, AcquisitionUsageID tinyint NOT NULL, OverlayFlags int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/SPCDefaultView/';

DROP TABLE IF EXISTS MEStemp.SPCRule;

Create External table IF NOT EXISTS MEStemp.SPCRule (SPCRuleID smallint NOT NULL, SPCRule nvarchar(100) NOT NULL, TAGMSTYPE1ID smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/SPCRule/';

DROP TABLE IF EXISTS MEStemp.SPCStatsStyle_CUS;

Create External table IF NOT EXISTS MEStemp.SPCStatsStyle_CUS (SPCStatsStyleID smallint NOT NULL, SPCStatsStyle nvarchar(100) NOT NULL, IsSystem tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/SPCStatsStyle_CUS/';

DROP TABLE IF EXISTS MEStemp.SPCVarChart;

Create External table IF NOT EXISTS MEStemp.SPCVarChart (SPCVarChartID int NOT NULL, SPCRuleID smallint NOT NULL, ProductID int, AcquisitionID smallint, MachineStageID smallint, VariableTypeID int, RfAttributeClassID tinyint, HeadNo smallint, RfAcquisitionClassID tinyint, TrendStartMSID int, TAGMSTYPE1ID smallint, ATTRIBUTETYPEID smallint, TAGMSVALUE1 nvarchar(508), CreateDT datetime NOT NULL, LoginID int NOT NULL, IsMsetInsert tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/SPCVarChart/';

DROP TABLE IF EXISTS MEStemp.Spec;

Create External table IF NOT EXISTS MEStemp.Spec (SpecID int NOT NULL, VariableID int NOT NULL, USL float, NOM float, LSL float, SpecTypeID smallint NOT NULL, IsEdited tinyint NOT NULL, RevisionNo smallint NOT NULL, DecPlaces tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Spec/';

DROP TABLE IF EXISTS MEStemp.Spec_;

Create External table IF NOT EXISTS MEStemp.Spec_ (SpecID int NOT NULL, LoginID smallint NOT NULL, Units nvarchar(40), URL float, LRL float, FromDT datetime NOT NULL, ToDT datetime NOT NULL, Comments nvarchar(2000), ModifiedDT datetime NOT NULL, DrawingNumber nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Spec_/';

DROP TABLE IF EXISTS MEStemp.SpecType;

Create External table IF NOT EXISTS MEStemp.SpecType (SpecTypeID smallint NOT NULL, SpecType nvarchar(100) NOT NULL, CUSTOMERID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/SpecType/';

DROP TABLE IF EXISTS MEStemp.SPMM_CalTools;

Create External table IF NOT EXISTS MEStemp.SPMM_CalTools (EDIT nvarchar(510), Site nvarchar(510), Control_No nvarchar(510), CSF_No float, AssetExternalControl_No nvarchar(510), Manufacturer nvarchar(510), EquipmentName nvarchar(510), Model_No nvarchar(510), Serial_No float, EquipmentLocation nvarchar(510), CalRequired nvarchar(510), CalFrequency nvarchar(510), CalDate datetime, CalDueDate datetime, Owner nvarchar(510), CalibrationSite nvarchar(510), Calibrator nvarchar(510), Status nvarchar(510), Isthelastcalibrationoutoftolerance nvarchar(510), DueDate datetime,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/SPMM_CalTools/';

DROP TABLE IF EXISTS MEStemp.SPMM_DMCost;

Create External table IF NOT EXISTS MEStemp.SPMM_DMCost (Oracle_PN nvarchar(100), Description nvarchar(510), Cost float,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/SPMM_DMCost/';

DROP TABLE IF EXISTS MEStemp.Supplier;

Create External table IF NOT EXISTS MEStemp.Supplier (SupplierID smallint NOT NULL, Supplier nvarchar(508) NOT NULL, SupplierCode nvarchar(508), AddressID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Supplier/';

DROP TABLE IF EXISTS MEStemp.sysdiagrams;

Create External table IF NOT EXISTS MEStemp.sysdiagrams (name nvarchar(256) NOT NULL, principal_id int NOT NULL, diagram_id int NOT NULL, version int, definition varbinary(-1),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/sysdiagrams/';

DROP TABLE IF EXISTS MEStemp.TagCauseAction;

Create External table IF NOT EXISTS MEStemp.TagCauseAction (TagCauseActionID int NOT NULL, CauseActionID int NOT NULL, TagTypeID smallint NOT NULL, TagValue nvarchar(508) NOT NULL, DT datetime NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/TagCauseAction/';

DROP TABLE IF EXISTS MEStemp.TagDef;

Create External table IF NOT EXISTS MEStemp.TagDef (TagDefID int NOT NULL, TagOwnerID int NOT NULL, RfTable nvarchar(60) NOT NULL, TagTypeID smallint NOT NULL, TagValue nvarchar(508) NOT NULL, DT datetime NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/TagDef/';

DROP TABLE IF EXISTS MEStemp.TagDefect;

Create External table IF NOT EXISTS MEStemp.TagDefect (TagDefectID int NOT NULL, IndividualDefectID int NOT NULL, TagTypeID smallint NOT NULL, TagValue nvarchar(508) NOT NULL, DT datetime NOT NULL, X smallint, Y smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/TagDefect/';

DROP TABLE IF EXISTS MEStemp.TagList;

Create External table IF NOT EXISTS MEStemp.TagList (TagListID smallint NOT NULL, ListName nvarchar(100) NOT NULL, VariantNo tinyint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/TagList/';

DROP TABLE IF EXISTS MEStemp.TagListValue;

Create External table IF NOT EXISTS MEStemp.TagListValue (TagListValueID int NOT NULL, TagListID smallint NOT NULL, TagListValue nvarchar(508) NOT NULL, DisplayOrder smallint NOT NULL, IsRetired tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/TagListValue/';

DROP TABLE IF EXISTS MEStemp.TagMS;

Create External table IF NOT EXISTS MEStemp.TagMS (TagMSID int NOT NULL, MeasurementSetID int NOT NULL, TagTypeID smallint NOT NULL, TagValue nvarchar(508) NOT NULL, DT datetime NOT NULL, TagTreeNodeID smallint, X smallint, Y smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/TagMS/';

DROP TABLE IF EXISTS MEStemp.TagPartInstance;

Create External table IF NOT EXISTS MEStemp.TagPartInstance (TagPartInstanceID int NOT NULL, PartInstanceID int NOT NULL, TagTypeID smallint NOT NULL, TagValue nvarchar(508) NOT NULL, DT datetime NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/TagPartInstance/';

DROP TABLE IF EXISTS MEStemp.TagProfile;

Create External table IF NOT EXISTS MEStemp.TagProfile (TagProfileID smallint NOT NULL, Name nvarchar(100) NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/TagProfile/';

DROP TABLE IF EXISTS MEStemp.TagTree;

Create External table IF NOT EXISTS MEStemp.TagTree (TagTreeID smallint NOT NULL, TagTree nvarchar(100) NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/TagTree/';

DROP TABLE IF EXISTS MEStemp.TagTreeNode;

Create External table IF NOT EXISTS MEStemp.TagTreeNode (TagTreeNodeID smallint NOT NULL, TagTreeNode nvarchar(100) NOT NULL, TagTreeID smallint, ParentTagTreeNodeID smallint, Depth tinyint, SortOrder tinyint, ImageURI nvarchar(510), NoCols tinyint, NoRows tinyint, SortExpression varchar(255), ImageWidth smallint, ImageHeight smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/TagTreeNode/';

DROP TABLE IF EXISTS MEStemp.TagTreeNodeImgLoc;

Create External table IF NOT EXISTS MEStemp.TagTreeNodeImgLoc (TagTreeNodeImgLocID smallint NOT NULL, TagTreeNodeID smallint NOT NULL, X tinyint NOT NULL, Y tinyint NOT NULL, Width tinyint NOT NULL, Height tinyint NOT NULL, MappedTagTreeNodeID smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/TagTreeNodeImgLoc/';

DROP TABLE IF EXISTS MEStemp.TagType;

Create External table IF NOT EXISTS MEStemp.TagType (TagTypeID smallint NOT NULL, RfDataTypeID tinyint NOT NULL, Name nvarchar(508) NOT NULL, TagTypeCode nvarchar(100), ISSYSTEM tinyint NOT NULL, TAGVIEW nvarchar(100),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/TagType/';

DROP TABLE IF EXISTS MEStemp.TagTypeInTagProfile;

Create External table IF NOT EXISTS MEStemp.TagTypeInTagProfile (TagProfileID smallint NOT NULL, TagTypeID smallint NOT NULL, EntryOrder smallint NOT NULL, CompulsoryConditions tinyint NOT NULL, IsDefaulted tinyint NOT NULL, IsEncrypted tinyint NOT NULL, TagListID smallint, IsReadOnly tinyint NOT NULL, RptQueryID int, TagTypeInTagProfileID int NOT NULL, TagTreeID int, RfTagDataSourceID tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/TagTypeInTagProfile/';

DROP TABLE IF EXISTS MEStemp.TMP_Checks_Done;

Create External table IF NOT EXISTS MEStemp.TMP_Checks_Done (ShiftID int NOT NULL, JobID int NOT NULL, AcquisitionContextID int NOT NULL, Checks_Done float,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/TMP_Checks_Done/';

DROP TABLE IF EXISTS MEStemp.TMP_Job_Shift_Downtime;

Create External table IF NOT EXISTS MEStemp.TMP_Job_Shift_Downtime (ShiftID int NOT NULL, MachineStageID int NOT NULL, JobID int NOT NULL, Downtime float,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/TMP_Job_Shift_Downtime/';

DROP TABLE IF EXISTS MEStemp.TMP_Jobs_In_Shifts;

Create External table IF NOT EXISTS MEStemp.TMP_Jobs_In_Shifts (ShiftID int NOT NULL, MachineStageID int NOT NULL, JobID int NOT NULL, PartID int NOT NULL, FromDT datetime NOT NULL, ToDT datetime NOT NULL, Downtime float, Total_Time float, Uptime float,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/TMP_Jobs_In_Shifts/';

DROP TABLE IF EXISTS MEStemp.Tool;

Create External table IF NOT EXISTS MEStemp.Tool (ToolID int NOT NULL, Tool nvarchar(508) NOT NULL, ToolSet nvarchar(508), Description nvarchar(508), ToolTypeID smallint NOT NULL, IsRetired tinyint NOT NULL, NoCavities tinyint NOT NULL, Variant1 nvarchar(200), Variant2 nvarchar(200), Variant3 nvarchar(200), Variant4 nvarchar(200), Variant5 nvarchar(200), Variant6 nvarchar(200), Variant7 nvarchar(200), Variant8 nvarchar(200), Variant9 nvarchar(200), Variant10 nvarchar(200), Variant11 nvarchar(200), Variant12 nvarchar(200), Variant13 nvarchar(200), Variant14 nvarchar(200), Variant15 nvarchar(200), PartID int, Variant16 nvarchar(200), Variant17 nvarchar(200), Variant18 nvarchar(200), Variant19 nvarchar(200), Variant20 nvarchar(200),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Tool/';

DROP TABLE IF EXISTS MEStemp.Tool_;

Create External table IF NOT EXISTS MEStemp.Tool_ (ToolID int NOT NULL, Supplier nvarchar(508), Reference1 nvarchar(508), Reference2 nvarchar(508), Reference3 nvarchar(508), Reference4 nvarchar(508), Reference5 nvarchar(508), Ident nvarchar(100),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Tool_/';

DROP TABLE IF EXISTS MEStemp.Tool_Custom;

Create External table IF NOT EXISTS MEStemp.Tool_Custom (ToolID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Tool_Custom/';

DROP TABLE IF EXISTS MEStemp.ToolAtLocation;

Create External table IF NOT EXISTS MEStemp.ToolAtLocation (ToolAtLocationID int NOT NULL, LocationID int NOT NULL, ToolID int NOT NULL, FromDT datetime NOT NULL, ToDT datetime NOT NULL, IsEdited tinyint NOT NULL, RevisionNo tinyint NOT NULL, EditedToolAtLocationID int, MovedByLoginID smallint NOT NULL, ModifiedDT datetime NOT NULL, Reference1 nvarchar(508), Reference2 nvarchar(508), Reference3 nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ToolAtLocation/';

DROP TABLE IF EXISTS MEStemp.ToolCavity;

Create External table IF NOT EXISTS MEStemp.ToolCavity (ToolCavityID int NOT NULL, CavityNumber tinyint NOT NULL, CavityName nvarchar(508), ToolID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ToolCavity/';

DROP TABLE IF EXISTS MEStemp.ToolCavityStateReason;

Create External table IF NOT EXISTS MEStemp.ToolCavityStateReason (ToolCavityStateReasonID smallint NOT NULL, ToolTypeID smallint NOT NULL, ToolCavityStateReason nvarchar(508) NOT NULL, Description nvarchar(508), CavityStatusCode nvarchar(100),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ToolCavityStateReason/';

DROP TABLE IF EXISTS MEStemp.ToolChangeReason;

Create External table IF NOT EXISTS MEStemp.ToolChangeReason (ToolChangeReasonID smallint NOT NULL, ToolTypeID smallint NOT NULL, ChangeReason nvarchar(508) NOT NULL, Description nvarchar(508), ChangeToRfToolStatusID tinyint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ToolChangeReason/';

DROP TABLE IF EXISTS MEStemp.ToolGroup;

Create External table IF NOT EXISTS MEStemp.ToolGroup (ToolGroupID smallint NOT NULL, ToolGroup nvarchar(508) NOT NULL, Description nvarchar(508), ToolGroupCode nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ToolGroup/';

DROP TABLE IF EXISTS MEStemp.ToolState;

Create External table IF NOT EXISTS MEStemp.ToolState (ToolStateID int NOT NULL, ToolID int NOT NULL, FromDT datetime NOT NULL, ToDT datetime NOT NULL, RfToolStatusID tinyint, LoginID smallint NOT NULL, Comments nvarchar(100), OpenedDT datetime NOT NULL, SavedDT datetime NOT NULL, IsEdited tinyint NOT NULL, RevisionNo tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ToolState/';

DROP TABLE IF EXISTS MEStemp.ToolType;

Create External table IF NOT EXISTS MEStemp.ToolType (ToolTypeID smallint NOT NULL, ToolType nvarchar(508) NOT NULL, ToolTypeCode nvarchar(100), Description nvarchar(508), Variant1TagListID smallint, Variant2TagListID smallint, Variant3TagListID smallint, Variant4TagListID smallint, Variant5TagListID smallint, Variant6TagListID smallint, Variant7TagListID smallint, Variant8TagListID smallint, Variant9TagListID smallint, Variant10TagListID smallint, Variant11TagListID smallint, Variant12TagListID smallint, Variant13TagListID smallint, Variant14TagListID smallint, Variant15TagListID smallint, Variant16TagListID smallint, Variant17TagListID smallint, Variant18TagListID smallint, Variant19TagListID smallint, Variant20TagListID smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ToolType/';

DROP TABLE IF EXISTS MEStemp.ToolUsage;

Create External table IF NOT EXISTS MEStemp.ToolUsage (ToolUsageID int NOT NULL, JobID int NOT NULL, MachineStageID smallint NOT NULL, ToolID int NOT NULL, FromDT datetime NOT NULL, ToDT datetime NOT NULL, ToolPosition nvarchar(508), ToolChangeReasonID smallint NOT NULL, IsEdited tinyint NOT NULL, RevisionNo tinyint NOT NULL, OperationID int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ToolUsage/';

DROP TABLE IF EXISTS MEStemp.ToolUsage_;

Create External table IF NOT EXISTS MEStemp.ToolUsage_ (ToolUsageID int NOT NULL, StartUsingComments nvarchar(508), ToolChangeComments nvarchar(508), StartLoginID smallint NOT NULL, ChangeLoginID smallint NOT NULL, OpenedStartDT datetime NOT NULL, OpenedChangeDT datetime NOT NULL, SavedStartDT datetime NOT NULL, SavedChangeDT datetime NOT NULL, OriginalLoginID smallint NOT NULL, EditedToolUsageID int, Reference1 nvarchar(508), Reference2 nvarchar(508), Reference3 nvarchar(508), Reference4 nvarchar(508), Reference5 nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/ToolUsage_/';

DROP TABLE IF EXISTS MEStemp.Unit;

Create External table IF NOT EXISTS MEStemp.Unit (UnitID smallint NOT NULL, Unit nvarchar(40) NOT NULL, Description nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Unit/';

DROP TABLE IF EXISTS MEStemp.UnitConversion;

Create External table IF NOT EXISTS MEStemp.UnitConversion (FromUnitID smallint NOT NULL, ToUnitID smallint NOT NULL, Multiplier float NOT NULL, UnitConversionID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/UnitConversion/';

DROP TABLE IF EXISTS MEStemp.UnitCount;

Create External table IF NOT EXISTS MEStemp.UnitCount (UnitCountID int NOT NULL, UnitCountTypeID smallint NOT NULL, PartID int NOT NULL, Quantity float NOT NULL, UnitID smallint NOT NULL, DT datetime NOT NULL, MachineStageID smallint NOT NULL, IsEdited tinyint NOT NULL, RevisionNo tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/UnitCount/';

DROP TABLE IF EXISTS MEStemp.UnitCount_;

Create External table IF NOT EXISTS MEStemp.UnitCount_ (UnitCountID int NOT NULL, CollectionQuantity float NOT NULL, CollectionUnitID smallint NOT NULL, Comments nvarchar(1024), LoginID smallint NOT NULL, OpenedDT datetime NOT NULL, SavedDT datetime NOT NULL, OriginalLoginID smallint NOT NULL, EditedUnitCountID int, Reference1 nvarchar(508), Reference2 nvarchar(508), Reference3 nvarchar(508), Reference4 nvarchar(508), Reference5 nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/UnitCount_/';

DROP TABLE IF EXISTS MEStemp.UnitCountType;

Create External table IF NOT EXISTS MEStemp.UnitCountType (UnitCountTypeID smallint NOT NULL, UnitCountType nvarchar(100) NOT NULL, Description nvarchar(508), MachineStageTypeID smallint NOT NULL, DefaultUnitID smallint NOT NULL, IsQuantityIn tinyint NOT NULL, IsQuantityOut tinyint NOT NULL, IsMaintenanceCount tinyint NOT NULL, IsConvertableToBaseUnits tinyint NOT NULL, AssetCountTypeID smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/UnitCountType/';

DROP TABLE IF EXISTS MEStemp.UnitCountTypeGroup;

Create External table IF NOT EXISTS MEStemp.UnitCountTypeGroup (UnitCountTypeGroupID smallint NOT NULL, UnitCountTypeGroup nvarchar(508) NOT NULL, Description nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/UnitCountTypeGroup/';

DROP TABLE IF EXISTS MEStemp.VarChartOC;

Create External table IF NOT EXISTS MEStemp.VarChartOC (VarChartOCID int NOT NULL, MeasurementSetID int NOT NULL, SPCChartControlLimitID int NOT NULL, SubgroupID varchar(20) NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/VarChartOC/';

DROP TABLE IF EXISTS MEStemp.Variable;

Create External table IF NOT EXISTS MEStemp.Variable (VariableID int NOT NULL, VariableTypeID int NOT NULL, ProductID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Variable/';

DROP TABLE IF EXISTS MEStemp.VariableType;

Create External table IF NOT EXISTS MEStemp.VariableType (VariableTypeID int NOT NULL, Name nvarchar(508) NOT NULL, ProductTypeID smallint NOT NULL, Description nvarchar(508), IsUnilateral tinyint NOT NULL, VariableTypeCode nvarchar(100), IsProcessParameter tinyint NOT NULL, VariableTypeClassID tinyint NOT NULL, ImageURI nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/VariableType/';

DROP TABLE IF EXISTS MEStemp.VariableTypeClass;

Create External table IF NOT EXISTS MEStemp.VariableTypeClass (VariableTypeClassID tinyint NOT NULL, VariableTypeClass nvarchar(508) NOT NULL, Description nvarchar(508), Priority tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/VariableTypeClass/';

DROP TABLE IF EXISTS MEStemp.VariableTypeGroup;

Create External table IF NOT EXISTS MEStemp.VariableTypeGroup (VariableTypeGroupID smallint NOT NULL, Name nvarchar(508) NOT NULL, Description nvarchar(508), VariableTypeGroupCode nvarchar(100), IsSmartGroup tinyint NOT NULL, SmartGroupPattern nvarchar(508),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/VariableTypeGroup/';

DROP TABLE IF EXISTS MEStemp.VarMeasurement;

Create External table IF NOT EXISTS MEStemp.VarMeasurement (VarMeasurementID int NOT NULL, SpecID int NOT NULL, MeasurementSetID int NOT NULL, Measurement float, Ignored tinyint NOT NULL, VariableTypeID int NOT NULL, ProductID int NOT NULL, HeadNo smallint NOT NULL, SampleNo smallint NOT NULL, PositionNo smallint NOT NULL, DT datetime NOT NULL, RfIsOSID tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/VarMeasurement/';

DROP TABLE IF EXISTS MEStemp.VarMeasurementP;

Create External table IF NOT EXISTS MEStemp.VarMeasurementP (VarMeasurementPID int NOT NULL, SpecID int, MeasurementSetPID int NOT NULL, Measurement float, Ignored tinyint NOT NULL, VariableTypeID int NOT NULL, ProductID int NOT NULL, HeadNo smallint NOT NULL, SampleNo smallint NOT NULL, PositionNo smallint NOT NULL, DT datetime NOT NULL, RfIsOSID tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/VarMeasurementP/';

DROP TABLE IF EXISTS MEStemp.Version;

Create External table IF NOT EXISTS MEStemp.Version (PlantDBVersion nvarchar(100) NOT NULL, Comments nvarchar(100), ModifiedDT datetime NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/Version/';

DROP TABLE IF EXISTS MEStemp.WO;

Create External table IF NOT EXISTS MEStemp.WO (WOID int NOT NULL, WONumber nvarchar(508) NOT NULL, RunNo smallint NOT NULL, PartID int NOT NULL, QuantityOrdered float NOT NULL, RfJobStatusID tinyint NOT NULL, DeadlineDT datetime NOT NULL, ActualStartDT datetime NOT NULL, ActualEndDT datetime NOT NULL, PlannedStartDT datetime NOT NULL, PlannedEndDT datetime NOT NULL, EstimatedStartDT datetime NOT NULL, EstimatedEndDT datetime NOT NULL, CustomerID smallint NOT NULL, IsDiscrete tinyint NOT NULL, Reference1 nvarchar(100), Reference2 nvarchar(100), Reference3 nvarchar(100), Reference4 nvarchar(100), Reference5 nvarchar(100), WORefID int NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/WO/';

DROP TABLE IF EXISTS MEStemp.WO_;

Create External table IF NOT EXISTS MEStemp.WO_ (WOID int NOT NULL, CustomerOrderNo nvarchar(508), LabelsPerInventoryPack smallint, ItemsPerInventoryPack float, ItemsPerLayer float, LayersPerInventoryPack int, PackingCode nvarchar(100), LabelTemplate nvarchar(508), LabelColour nvarchar(100), EANCode char(25), CusStartNum int, CusEndNum int, CustomerPartNo nvarchar(508), CustomerPartDescription nvarchar(508), LabelText1 nvarchar(508), LabelText2 nvarchar(508), LabelText3 nvarchar(508), PrinterURL nvarchar(2048), BOMVariantID int,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/WO_/';

DROP TABLE IF EXISTS MEStemp.WO_custom;

Create External table IF NOT EXISTS MEStemp.WO_custom (WOID int NOT NULL, BaseWONumber varchar(254), Txn_date datetime, Wip_Entity_ID int, Alternate_Bom nvarchar(100), WO_Status nvarchar(40), IsBtoCto tinyint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/WO_custom/';

DROP TABLE IF EXISTS MEStemp.WORef;

Create External table IF NOT EXISTS MEStemp.WORef (WORefID int NOT NULL, WOReference nvarchar(508) NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/WORef/';

DROP TABLE IF EXISTS MEStemp.WorkInstructionProcedure;

Create External table IF NOT EXISTS MEStemp.WorkInstructionProcedure (WorkInstructionProcedureID smallint NOT NULL, WorkInstructionProcedure nvarchar(508) NOT NULL, WorkInstructionRef nvarchar(100), PartID int NOT NULL, IsEdited tinyint NOT NULL, FromDT datetime NOT NULL, ToDT datetime NOT NULL, RevisionNo tinyint NOT NULL, LoginID smallint NOT NULL, ModifiedDT datetime NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/WorkInstructionProcedure/';

DROP TABLE IF EXISTS MEStemp.WorkInstructionResult;

Create External table IF NOT EXISTS MEStemp.WorkInstructionResult (WorkInstructionResultID int NOT NULL, WorkInstructionStepID int NOT NULL, OperationID int NOT NULL, DT datetime NOT NULL, Comments nvarchar(2048), LoginID smallint NOT NULL,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/WorkInstructionResult/';

DROP TABLE IF EXISTS MEStemp.WorkInstructionStep;

Create External table IF NOT EXISTS MEStemp.WorkInstructionStep (WorkInstructionStepID int NOT NULL, WorkInstructionStep nvarchar(508) NOT NULL, WorkInstructionProcedureID smallint NOT NULL, OperationTypeID smallint, StepOrder float NOT NULL, RfSpecialActionTypeID int NOT NULL, AcquisitionVariantID smallint,  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/WorkInstructionStep/';

DROP TABLE IF EXISTS MEStemp.xxxHipotData;

Create External table IF NOT EXISTS MEStemp.xxxHipotData (SERIAL_NUMBER nvarchar(100), CONT_A decimal(18,2), CONT_B decimal(18,2), CONT_C decimal(18,2), HIPOT decimal(18,2), STATION nvarchar(100), DATETIME datetime, USERNAME nvarchar(100), REMARKS nvarchar(200),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/xxxHipotData/';

DROP TABLE IF EXISTS MEStemp.xxxHipotData_Buffer;

Create External table IF NOT EXISTS MEStemp.xxxHipotData_Buffer (SERIAL_NUMBER nvarchar(100), CONT_A decimal(18,2), CONT_B decimal(18,2), CONT_C decimal(18,2), HIPOT decimal(18,2), STATION nvarchar(100), DATETIME datetime, USERNAME nvarchar(100), REMARKS nvarchar(200),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/xxxHipotData_Buffer/';

DROP TABLE IF EXISTS MEStemp.xxxIPN;

Create External table IF NOT EXISTS MEStemp.xxxIPN (PartNumber nvarchar(100), IPN nvarchar(500),  modcoID int NOT NULL) row format delimited fields terminated by ',' lines terminated by '\n' stored as textfile location '/MEStemp/xxxIPN/';


